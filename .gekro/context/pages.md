# Pages & Routes

Every route in `apps/web/src/pages/`. Path is implied — prepend `apps/web/src/pages/`.

## Static top-level

| Route | File | Layout | Data sources | Notes |
|---|---|---|---|---|
| `/` | `index.astro` | `BaseLayout` | `getAllPosts()` (top 3) + `getCollection('experiments')` (top 3 by `startDate`) + `getTopicCounts()` (top 8) | Hero = `HeroCanvasNeural` + `HeroText`. Homepage GSAP animations wrapped in `requestIdleCallback` + bound to `astro:after-swap`. Newsletter form is mock-submit only (real submission TBD). |
| `/about` | `about.astro` | `BaseLayout` | Static content + `LabGallery` | Uses SVG schematics, not photos. |
| `/contact` | `contact.astro` | `BaseLayout` | `ContactForm` (Turnstile-gated) | |
| `/now` | `now.astro` | `BaseLayout` | Static | "Now page" — what I'm working on currently. |
| `/slides` | `slides.astro` | `BaseLayout` | Static listing | Placeholder for Reveal.js-based decks (blueprint goal — not fully wired up yet). |
| `/404` | `404.astro` | `BaseLayout` | Static | Custom not-found page. |

## Blog

| Route | File | Layout | Data sources | Notes |
|---|---|---|---|---|
| `/blog` | `blog/index.astro` | `BaseLayout` | `getAllPosts()` | Filterable listing. |
| `/blog/[slug]` | `blog/[slug].astro` | `BlogLayout` | Content Collection or Sanity (via `getAllPosts()`) | `getStaticPaths` from collection; rendered via `PortableText` for Sanity bodies, markdown directly for local. Light-reader theme toggle available here. |

## Experiments

| Route | File | Layout | Data sources | Notes |
|---|---|---|---|---|
| `/experiments` | `experiments/index.astro` | `BaseLayout` | `getCollection('experiments')` | Cards should not have hardcoded `opacity-0` — GSAP handles entrance (lesson from 2026-03-24 invisible-cards incident). |
| `/experiments/[slug]` | `experiments/[slug].astro` | `ExperimentLayout` | Content Collection | **Local-first since 2026-03-24** — Sanity fetch was fragile for this route. Open backlog: every new experiment should include a "proof" layer (screenshot or `render_diffs` block). |

## Apps

| Route | File | Layout | Data sources | Notes |
|---|---|---|---|---|
| `/apps` | `apps/index.astro` | `BaseLayout` | `getCollection('apps')` | Search + category-chip filter UI is inline in this page (there is no separate `AppFilters.astro`). |
| `/apps/[slug]` | `apps/[slug].astro` | `AppLayout` | Content Collection (`apps`) | Dynamic-imports the per-app `Calculator.astro` from `apps-private/`. |

## Stack

| Route | File | Layout | Data sources | Notes |
|---|---|---|---|---|
| `/stack` | `stack/index.astro` | `BaseLayout` | `getCollection('stack')` | Third-party tool reviews. |
| `/stack/[slug]` | `stack/[slug].astro` | `StackLayout` | Content Collection (`stack`) | Renders verdict, pros/cons, referral footer. |

## News

| Route | File | Layout | Data sources | Notes |
|---|---|---|---|---|
| `/news` | `news/index.astro` | `BaseLayout` | `getCollection('news')` | Daily AI-briefing feed. |
| `/news/[slug]` | `news/[slug].astro` | `BaseLayout` | Content Collection (`news`) | Per-briefing detail. |

## Topics

| Route | File | Layout | Data sources | Notes |
|---|---|---|---|---|
| `/topics/[topic]` | `topics/[topic].astro` | `BaseLayout` | `getAllPosts()` + filter by topic | Topic slugs derive from `TOPIC_MAP` in `lib/utils/posts.ts`. |

## API / feeds

| Route | File | Purpose | Notes |
|---|---|---|---|
| `/api/posts.json` | `api/posts.json.ts` | Public JSON endpoint of all posts. | **Must always stay live and return valid JSON** (SEO/AI discoverability rule). Fields: title, slug, summary, topics, aiSummary, publishedAt, url. |
| `/rss.xml` | `rss.xml.ts` | RSS feed via `@astrojs/rss`. | |
| `/sitemap-index.xml` + `/sitemap-0.xml` | generated by `@astrojs/sitemap` integration | Auto-generated on build. | `site: 'https://gekro.com'` is the canonical base. |
| `/llms-full.txt` | `llms-full.txt.ts` | Full machine-readable content manifest for LLM crawlers. | Linked from `public/llms.txt`. |
| `/news-rss.xml` | `news-rss.xml.ts` | RSS feed scoped to the daily AI-news briefings. | |
| `/news-sitemap.xml` | `news-sitemap.xml.ts` | News-specific sitemap. | |
| `/og/{apps,blog,experiments,news,stack}/[slug].png` | `og/**/[slug].png.ts` | Dynamic per-entry Open Graph images, generated at build. | One generator per content type. |

## Layout chain

```
BaseLayout.astro
├─ <head>
│  ├─ GA (env-conditional: PUBLIC_GA_MEASUREMENT_ID)
│  ├─ GTM snippet (hardcoded GTM-MBGML7FV, deferred 3500ms)
│  ├─ SEOHead (required)
│  └─ <ClientRouter />   (View-Transitions router; renamed from <ViewTransitions /> in Astro 5+)
├─ <body>
│  ├─ GTM noscript iframe
│  ├─ Header
│  ├─ <div id="swup"><slot /></div>
│  ├─ Footer
│  ├─ astro:after-swap GSAP fade-in on <main>
│  ├─ Cloudflare Web Analytics beacon (hardcoded token)
│  └─ LabTerminal
```

`BlogLayout` and `ExperimentLayout` both wrap `BaseLayout`, add `<article>` structure, `TLDR`, `PostHeader`/experiment meta, `PortableText` or markdown body, `ReadingProgress`, `RelatedPosts`, `GiscusComments`.

## Data-flow rules

- **Don't fetch posts outside `lib/utils/posts.ts`.** Use `getAllPosts()` or `getTopicCounts()`. The centralized fetcher is a hard decision from 2026-03-24 (duplicated-logic maintenance hazard).
- **Content Collections are the primary source of truth.** Sanity is supplementary; the site must build local-only when Sanity env is missing (`client.ts` returns a rejecting stub).
- **Zod schema in `src/content/config.ts` is the gate.** If you add a frontmatter field to a markdown post or experiment, update the Zod schema first or the build will fail on content load.
- **Sanity schema must mirror Zod schema** for shared fields. When changing one, update the other (apps/studio/schemas/post.ts ↔ apps/web/src/content/config.ts).
