# Gekro Lab — Decision Log

This log tracks architectural and technical decisions. Before implementing counter-intuitive requests, the agent will cross-reference this log and request an **Override** if a conflict is found.

| Date/Time | Decision | Rationale | Implications | Override Count |
|---|---|---|---|---|
| 2026-03-24 00:10 | Use "Technical SVG Schematics" for Lab Gallery | Physical photos are hard to maintain and can feel dated. SVG schematics reinforce the "Engineering Lab" aesthetic. | Consistency in visual storytelling across About/Gallery pages. | 0 |
| 2026-03-24 00:40 | Migrate Experiments to Content Collections | Sanity-based fetching proved fragile for core site features. Local Markdown files provide better build-time stability and SEO. | Local-first architecture; removed Sanity dependency for experiments. | 0 |
| 2026-03-24 01:10 | "Authoritative Engineering Lab" Persona | Shift site tone from "Personal Blog" to "Live Engineering Log." | Impacted all copy, footer heartbeat, and technical depth in posts. | 0 |
| 2026-03-24 01:15 | Continuous Issue & Decision Logging | To prevent development cycles and regressions per user request. | Agent must read logs before every planning/execution phase. | 0 |
| 2026-03-24 02:00 | Gekro "Deep Dive" Content Standard | Establish a rigid structure for technical posts to ensure authority, depth, and persona consistency. | Rewriting 8 posts; impacts all future content structure and voice. | 0 |
| 2026-03-24 04:45 | Centralized Post-Fetching via `posts.ts` | Duplicated Sanity+local merge pattern across 4 pages was a maintenance hazard. Single `getAllPosts()` utility eliminates this. | All pages/API now source posts through one path. Must update `posts.ts` for any schema changes. | 0 |
| 2026-03-24 04:45 | Env-Var-Only Secrets | All API keys, analytics IDs, and service keys must come from env vars (`import.meta.env` / `process.env`). No hardcoded fallbacks in source. | Added `.env.example` as the single source of truth for required vars. | 2 |
| 2026-03-24 13:45 | Content Integrity Standard | All blog/experiment content must be cross-referenced for hardware consistency (Pi count, specs), provider naming consistency, and factual accuracy. Unsubstantiated claims must be softened or evidenced. Code examples must be complete (no `...` or `your_module`). | Applied to all 11 content files. Standardized on 3× Pi 5 16GB + M.2 256GB across all articles. | 0 |
| 2026-03-28 16:35 | Deep Dive Content Standard V2 | Adopt V2 structure (Hook, Architecture, Build, Tradeoffs, Where This Goes) and "Rohit" persona. Rigidly enforce first-person, no clichés, and 6-8 min reading time. | Mandatory adherence for all future technical posts. Updated SKILL.md and template. | 0 |
| 2026-03-30 06:43 | Hardcoded GTM Tag | User explicitly overrode Env-Var-Only Secrets for Google Tag Manager to resolve analytics issue directly in the view. | Hardcoded GTM ID in BaseLayout.astro. Removed `PUBLIC_GTM_ID` from `.env.local` and `.env.example`. | 0 |
| 2026-03-30 06:53 | Hardcoded Cloudflare Tag | User explicitly overrode Env-Var-Only Secrets for Cloudflare Web Analytics token. | Hardcoded Cloudflare beacon snippet in BaseLayout.astro. | 0 |
| 2026-03-31 23:30 | Localized Light Reader Mode | The reading pages (Blog/Experiments) required a light mode toggle (like deepwiki) without changing the global dark mode. Implemented View Transitions API and scoped CSS variables. Later escalated light theme binding to `document.body` to resolve global fixed header contrast issues. | Requires a cleanup script bound to `astro:after-swap` to strip the light theme from the body if navigating back to the homepage. | 0 |
| 2026-04-01 00:10 | Performance Overhaul vs Aesthetics | Addressed 34-second TBT via `requestIdleCallback` for WebGL and GSAP, but actively declined removing the GSAP Hero text fade-in despite Lighthouse Element Render Delay penalty. | The premium "wow" factor of the entrance animation overrides achieving a literal 100/100 LCP score. | 1 |
| 2026-04-11 | Portable Blog Post Generator + `blog-drafts/` convention | A single self-contained `BLOG_POST_GENERATOR.md` can be dropped into any active repo so the AI agent scans the codebase in-place and drafts a Deep Dive that matches gekro's current voice — avoiding the friction of cloning the target repo into gekro. The only valid draft target is `./blog-drafts/YYYY-MM-DD-slug.md` at the root of the working repo (gitignored on first run), never inside any publishable content folder. Canonical source lives at `.gekro/docs/BLOG_POST_GENERATOR.md`; downstream copies are re-synced manually. | All future Deep Dives authored outside gekro flow through this generator. Drafts never hit `apps/web/src/content/blog/` until the user manually moves them after review. Generator self-updates by fetching the 2 most recent live posts as style reference before every run, and proposes amendments to itself in the gekro canonical after each session. | 0 |
| 2026-04-11 | Deep Dive Standard V2.1 — Section V split | Section V of the Deep Dive structure now has two valid headings instead of one: `## What I Learned` for retrospective / hands-on posts (default when writing alongside real code), and `## Where This Goes` for forward-looking / speculative / framing posts (e.g. green tech predictions, exploratory research). Codifies the pattern already present in `pi5-mcp-orchestrator.md` (`What I Learned`) vs `ai-codes-like-genius.md` (`Where This Goes`). | Updated `deep-dive-standard.md` to V2.1 and `BLOG_POST_GENERATOR.md` Section 5.V in lockstep. No retroactive rewrites of existing posts required — both forms are now canonical. | 0 |
| 2026-04-11 | Deep Dive Standard V2.2 — Length cap relaxed to soft target | The 6–8 minute reading-time target is now a default, not a hard ceiling. Content-rich posts with layered architectures, multi-stage builds, or multiple distinct war stories may run longer when the subject genuinely demands it. Padding remains forbidden — before exceeding the target, the writer must audit for filler (restated points, ceremonial transitions, hedging) and cut it. Overshooting is explicitly framed as a rare exception, not a license to sprawl. | Updated `deep-dive-standard.md` Section 2 to V2.2 and `BLOG_POST_GENERATOR.md` Section 5 "Length discipline" subsection + Section 6 frontmatter comment + Section 11 Example D phrasing. No retroactive rewrites of existing posts — all 11 current posts already fall within 5–8 minutes. | 0 |
| 2026-04-15 | Replace Beehiiv iframe embed with native dark-styled form | Beehiiv's iframe is cross-origin — their internal CSS forces a white background that cannot be overridden from outside (same-origin policy). The hardcoded `height="339"` also clipped the subscribe button. A native form POSTing to the same Beehiiv subscribe endpoint (`subscribe-forms.beehiiv.com/{id}/submissions`) via `fetch()` with `mode: 'no-cors'` gives total visual control and identical functionality. No-cors means an opaque response; success is shown optimistically (any non-thrown response = success; only caught network errors trigger an error state). | `NewsletterEmbed.astro` no longer contains an `<iframe>`. The `PUBLIC_BEEHIIV_PUBLICATION_ID` env var is still required. `data-wired` guard prevents listener stacking on `astro:after-swap`. Dev fallback (no publication ID) shows a local-only success message. | 0 |
| 2026-04-17 | Code-review sweep: 5 targeted refactors | Full code-review pass (with a verification sub-agent triage to weed out hallucinated findings) identified 5 real improvements worth landing together: (1) escape user input in LabTerminal command echo; (2) extract WORDS_PER_MINUTE constant + delete dead duplicate of `calculateReadingTime` from posts.ts; (3) consolidate Pagefind UI CSS variables from SearchWidget + SearchModal into a scoped block in global.css; (4) replace `any`-typed Props in BlogLayout/ExperimentLayout with `FormattedPost` and a new `ExperimentMeta` interface; (5) wire GiscusComments to a `gekro:theme-change` CustomEvent from ThemeToggle so comment theme tracks in-page toggle. Dropped findings: the agent flagged several false positives (duplicate copy handlers, missing startViewTransition guard, setInterval accumulation, RAF listener leaks) that ground-truth verification disproved — left unchanged. | Review cadence: when touching multiple components, spend a review pass catching small issues together rather than one-off follow-ups. | 0 |
| 2026-04-16 | Canonical URL convention: no trailing slash (except root) | Google Search Console reported "Alternate page with proper canonical tag" for every page because `Astro.url.href` produces `/blog/` while Google crawls `/blog`. Adopting no-trailing-slash as the site-wide canonical convention eliminates the ambiguity. Three enforcement points: `SEOHead.astro` (meta tag), `astro.config.mjs` sitemap serializer (XML), `public/_redirects` (Cloudflare edge 301). Root `/` is the only exception — it has no trailing segment to strip. | All canonical `<link>` tags now read `https://gekro.com/blog` (not `/blog/`). Sitemap entries match. Cloudflare 301s absorb stray trailing-slash requests before they hit SSG-served pages. | 0 |
| 2026-04-17 | **REVERSED** 2026-04-16: canonical = trailing-slash (natural form) | The no-trailing-slash strategy was incompatible with Astro's directory-format build output (`dist/blog/slug/index.html`) because Cloudflare Pages auto-redirects non-slash URLs back to the slash form, looping against our strip-trailing-slash `_redirects` rule. Root cause of the loop was a stack-up of three well-intentioned pieces (SEOHead normalisation + sitemap serializer + _redirects 301) that together fought CF Pages' built-in directory canonicalisation. Also: the original GSC signal ("Alternate page with proper canonical tag") was Google *correctly* respecting the canonical tag, not an indexing failure — so the 2026-04-16 change was solving a non-problem. | Canonical, sitemap, and `_redirects` all reverted to the natural trailing-slash form. If the GSC "Alternate" signal genuinely becomes a problem, the correct fix is either `trailingSlash: 'never'` + `build.format: 'file'` in astro.config (so output is `slug.html` instead of `slug/index.html`), or just accept Google's canonical-respecting behaviour. | 0 |
| 2026-04-17 | SEO + GEO sweep — no content edits, no new deps | Full structured-data + discoverability pass driven by "rank higher on Google/Bing/LLMs". Landed: (1) expanded SEOHead with article:* OG tags, twitter:creator, per-post ogImage plumbing, `<link rel=alternate>` for /llms-full.txt and /rss.xml; (2) expanded BlogPosting/TechArticle JSON-LD with image, dateModified, wordCount, keywords, articleSection, mainEntityOfPage; (3) BreadcrumbList on posts/experiments/topics/blog-index/experiments-index; (4) WebSite (with SearchAction) + Organization + Person global schema via `GlobalSchema.astro` on homepage; ProfilePage + Person on /about anchoring `/about/#person` @id; CollectionPage + ItemList on /blog, /experiments, /topics/[slug]; (5) sitemap `serialize()` hook emitting lastmod from frontmatter dates + per-URL-pattern priority/changefreq; (6) `/llms-full.txt` endpoint — flat plain-text manifest of every post+experiment (aiSummary preferred), plus regenerated public/llms.txt and expanded robots.txt allowlist (OAI-SearchBot, Applebot-Extended, CCBot, etc.); (7) per-post OG image endpoints at `/og/blog/[slug].png` and `/og/experiments/[slug].png` rendered from SVG via sharp — zero new deps; (8) custom /404 page with recent-posts fallback. **Zero article-content edits** per user directive "do not touch article content"; audit of title/description/aiSummary lengths written to `.gekro/logs/seo-audit-2026-04-17.md` as a report for future Cross-Sync pass. | Every page ships with image-ready, AI-parseable structured data. GEO crawlers (GPTBot, ClaudeBot, Perplexity, etc.) now have a single canonical manifest at `/llms-full.txt` with aiSummary per entry. Build is green (43 pages, up from 42). Post-deploy: verify via curl that `/llms-full.txt`, `/og/blog/<slug>.png`, and sitemap lastmod render on gekro.com. | 0 |
| 2026-04-15 | Visual "wow" features — no new dependencies | Added 5 polish features to transform the site from "well-built blog" to "engineer who sweats details": (1) Particle cursor repulsion in Three.js hero (`Vector3.unproject()` + inverse-square force, desktop only); (2) 3D perspective tilt on PostCard and ExperimentCard (±8°, GPU-composited via `will-change: transform`, reduced-motion gated); (3) CursorFollower — 12px accent dot with lerp trailing, morphs to 40px ring on interactive elements, `pointer:fine` + `min-width:768px` only; (4) SearchModal — Cmd+K/Ctrl+K Pagefind overlay, lazy-loaded on first open; (5) Discoverability hints — Footer keyboard shortcut hint at 30% opacity, ThemeToggle one-time pulse animation (sessionStorage-gated). Zero new npm dependencies. Mobile impact: zero (all features CSS/JS-gated to desktop pointer devices). | `CursorFollower.astro` and `SearchModal.astro` are new global components mounted in `BaseLayout.astro` outside `#swup`, persisting across View Transitions. `ExperimentCard.astro` now has full JS block (was previously no-JS). `PostCard.astro` lost `hover:translate-y-[-4px]` Tailwind class (replaced by JS transform). | 0 |
| 2026-04-19 | Introduce `/apps` section — single-session client-only mini-tools | gekro is positioning as an AI-engineer's public lab + portfolio front. The blog is the lab notebook; apps are the working bench. Every app must be (a) something Rohit personally uses, (b) shareable as static client-side code with no backend or user data, (c) accompanied by a methodology blog post that two-way-links to the app. First app: LLM cost calculator with three modes — API cost / local hardware TCO (AI Hat 2 vs RTX 5060 vs cloud) / side-by-side. Architecture mirrors the blog pattern exactly: new `apps` content collection (Zod schema in `content/config.ts`), `pages/apps/index.astro` grouped by `category` frontmatter, `pages/apps/[slug].astro` dynamic route, `AppLayout.astro` extending `BaseLayout`. Hard guardrails to prevent overengineering: static-only (no APIs/edge functions/DB), localStorage for preferences only (never user data), no new npm deps until 3+ apps demand the same one, one file per app until it grows past ~400 lines, no premature shared "calculator framework". Hardware-comparison apps use a curated benchmark JSON (`{ model, hardware, quantization, tokens_per_sec, watts }`) sourced from published numbers + Rohit's own runs — never predict throughput from raw specs. Pricing data lives in a single `pricing.json` per app with a "last verified" date shown in the UI. Cap on cadence: one new app per month until traffic data justifies more. | New collection + routes + layout to ship, but the pattern is fixed and reusable for every subsequent app. Apps are open-sourced as part of the repo — companion to the blog, not a separate codebase. Failure mode to watch: data freshness drift (model prices change monthly) and feature creep within a single app (resist export/share/PDF until usage justifies). | 0 |
| 2026-04-19 | `/apps` scope expanded to platform — 20+ apps planned | User confirmed roadmap of 20+ apps. Scale inverts the earlier "no shared utils until 3 apps" guardrail: shared abstractions are now justified upfront by real cross-cutting requirements, not speculation. Locked-in cross-cutting features that ship from day one: (1) **CSV export** on every app — `components/apps/shared/csv.ts` exposes `exportRows(rows, filename)`; (2) **URL-state sync** on every app — `shared/url-state.ts` encodes inputs in querystring so scenarios are bookmark-able and link-shareable without auth (bigger democratization win than CSV alone); (3) **Shared UI primitives** — `NumberInput`, `Select`, `Tabs`, `ResultCard`, `CopyButton`, `ExportButton`, `ResetButton` — all Tailwind-token-based, no custom CSS; (4) **AppShell** with toolbar (CSV + Copy + Share-link + Reset) and "last verified" stamp; (5) **Landing page** with vanilla-JS search + category filter chips (no Fuse.js, no Pagefind for /apps — overkill for 20–50 items). Per-app UX standard locked: sensible defaults so results render instantly (no blank state), live recalc on input (no submit button), mobile-first numeric inputs, methodology link to companion blog post. **Categories taxonomy v1 (locked, additive only):** `ai`, `infra`, `ev`, `trading`, `dev`, `finance`. Every app declares one. **Cadence still capped:** 1 app/month until traffic data justifies more. The full canonical spec lives in `.gekro/docs/apps-platform-standard.md`. | All shared utilities ship with the platform scaffold (not lazily added later). Adding an app becomes: 1 markdown file + 1 island component + (optional) 1 data file. Resist any expansion to the shared layer beyond the locked list — new shared primitives need an explicit decision-log entry. | 0 |
| 2026-04-19 | Phase 1 data approved — LLM Cost Calculator (App #1) | User approved researched benchmark + pricing data. Key decisions confirmed: (1) Pi hardware = Pi AI HAT+ 2 (Hailo-10H, 40 TOPS) scoped to 1.5–3B models only — Hailo-8/8L cannot run 7B+ models; (2) OpenAI = GPT-5.4 + GPT-5.4 mini (current, not deprecated Aug-2025 GPT-5); (3) Null benchmarks show "No published data" greyed out — no estimation. Approved tagline: *"Free tools I built for myself. Sourced data. No login."* — baked into standard §1. Parking lot additions: App #2 Text-to-Markdown Visualizer (dev category, dep decision needed for Markdown parser), App #3 Voice Recorder + Transcript Generator (ai category, dep decision needed for Whisper.js vs Web Speech API). Full parking lot tracked in `.gekro/docs/apps-parking-lot.md`. | Build proceeds: Phase 2 scaffold + Phase 3 Calculator in one pass. | 0 |
| 2026-04-20 | 11 dev-tool mini-apps shipped — hand-rolled micro-faker, zero backend | Added apps: json-formatter, text-diff, jwt-decoder, password-generator, dummy-data-generator, code-snippet-png, color-toolkit, gradient-generator, image-compressor, csv-to-json, device-info. All client-side. Deps: diff, papaparse, browser-image-compression, html-to-image, shiki — all MIT/Apache. Micro-faker hand-rolled (~10 KB) instead of @faker-js/faker (1.7 MB). Each app follows `astro:after-swap`+`_xyzInit` guard pattern. | All 11 registered in [slug].astro CALCULATOR_MAP. Build clean at 73 pages. | 0 |
| 2026-04-21 | Apps catalogue rebalance: 9 new on-brand apps + Featured/Other split, no-delete rule | External audit found the apps section under-represented the "AI engineer's lab" positioning — 18/31 apps were generic dev utilities, only 3/31 were AI-engineer-specific. Two-pronged response. **Restructure:** apps page now sorts by category priority (`ai → trading → ev → infra → dev → finance` then `health → fun`), splitting into two visual sections — "Featured" (AI/infra/eng) and "Other Tools" (personal-utility apps Rohit built so he wouldn't need to visit other sites). When a specific filter chip is active, section headers hide so the grouping only shows in the default "all" view. **Hard rule logged in memory:** apps are NEVER deleted — URL preservation, SEO, link continuity matter; if an app feels off-brand, re-categorise to lower priority instead. Cadence cap (1 app/month) is now informal — apps ship as ideas arrive (e.g., QR code generator was built same day Rohit needed one). **9 new apps shipped, all featured-tier:** Tokenizer Visualizer, RAG Chunk Inspector, GPU VRAM Calculator, EV Charging Cost Calculator, Docker Compose Visualizer, JSON Schema → LLM Tool Definition, System Prompt Linter, Context Window Visualizer, systemd Unit File Generator. All zero-new-deps. All follow standard pattern (idempotency guard, app:reset/copy/export listeners, live recalc, prefilled examples). Catalogue now 40 apps total; AI category jumped from 3 → 9. | Memory updated with no-delete rule (`project_apps_initiative.md`) — overrides any future agent instinct to "trim" the catalogue. SEO note: apps still get per-slug OG images (`/og/apps/[slug].png`) auto-generated. Build clean at 84 pages, zero TS errors. Three rounds of testing landed: build verification, bug audit (1 real fix: gpu-vram-calculator NaN guard for custom params), UX polish (tokenizer + RAG inspector render caps for huge inputs, system-prompt-linter score interpretation legend). | 0 |
| 2026-04-20 | Full SEO/GEO sweep — 20+ targeted fixes, zero blog-content edits | Comprehensive SEO pass to maximize traffic and AI-crawler discoverability. Changes: (1) SEOHead: added `noindex` prop + `<meta name="robots">` tag (default: index,follow); removed redundant `<meta name="title">`; improved og:image:alt and twitter:image:alt; improved default site description. (2) BaseLayout: `noindex` prop threaded through to SEOHead. (3) 404.astro: `noindex={true}` wired — was previously missing despite the comment saying it existed. (4) Experiment frontmatter: fixed 3 `startDate` values from human-readable "Month Year" to ISO 8601 (YYYY-MM-DD) — was causing invalid JSON-LD `datePublished`. ExperimentLayout now formats date for display using `toLocaleDateString`. (5) JSON-LD author @id: added `@id: "https://gekro.com/about/#person"` to all 3 layouts (Blog, Experiment, App) — enables entity resolution by Google's Knowledge Graph. (6) JSON-LD publisher logo: upgraded from `favicon.svg` to `og/default.png` (1200×630) + added `@id: "https://gekro.com/#organization"` for entity linking. (7) BlogLayout dateModified: uses `updatedAt` frontmatter field when present (falls back to publishedAt); `updatedAt` added as optional field to blog Zod schema. (8) RSS: fixed link trailing slash; added `<author>` per item + `<managingEditor>` + `<webMaster>` to channel. (9) Topics breadcrumb: fixed position-2 item from `/blog/` to `/topics/`. (10) Blog index description: fixed typo "systems build" → rewrote to keyword-rich 155-char description. (11) Meta descriptions: rewrote all static page descriptions (experiments index, about, now, contact, slides, homepage, apps index, topics) to include target keywords and person name. (12) astro.config.mjs: added `filter()` to exclude `/og/` and `/api/` from sitemap; added apps to `scanCollection`; added `/apps/` to index tier (priority 0.9) and individual apps to 0.7 tier. (13) Per-app OG images: new `/og/apps/[slug].png.ts` endpoint mirrors blog/experiment pattern — each app now gets a branded preview card. AppLayout wired to use `ogImage=/og/apps/${slug}.png`. (14) llms.txt: added `/now`, `/slides`, `/apps` navigation entries; expanded topic cluster list; updated date. | Zero blog-content edits (per user directive). All changes land in layout/config/static/frontmatter-of-apps files. Post-deploy verify: (a) Google Rich Results Test on a blog post — check dateModified ≠ datePublished after first `updatedAt` use; (b) curl /og/apps/llm-cost-calculator.png — confirm 200 PNG; (c) curl /rss.xml — confirm `<author>` elements present; (d) GSC sitemap resubmit. | 0 |
| 2026-04-19 | Open-source licensing: MIT (code) + CC BY-NC 4.0 (content) + per-app override | Repo was previously unlicensed (default "all rights reserved" — technically blocking all reuse). Adopting dual-license pattern: (1) **Root `LICENSE` (MIT)** — covers all code in the repo, including all apps. Standard, attribution-required (must include copyright + license text), allows commercial reuse. (2) **Root `LICENSE-CONTENT` (CC BY-NC 4.0)** — covers blog posts, app copy, methodology text. Requires attribution, blocks commercial reuse (deters SEO-farm scraping). (3) **Per-app `license` frontmatter field** in `apps` collection schema, defaults to `MIT` — allows future override on a per-app basis if a specific app ships data/dependencies that require different terms. (4) **`AttributionFooter.astro`** rendered on every app page reading the frontmatter license value: "© 2026 Rohit Burani · {license} · Built at gekro.com · View source on GitHub". Visible attribution backs up the legal license with social pressure. (5) **`README.md` updated** to state both licenses, link to LICENSE files, and explain the per-app override convention. | Repo state changes from "unlicensed/all rights reserved" to "dual-licensed open source." Standard pattern for code+content sites. Per-app override exists but should be rare — overuse fragments the licensing story and confuses contributors. Any per-app license that isn't MIT requires its license text added to a `LICENSES/` folder at root. | 0 |
