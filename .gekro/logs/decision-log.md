# Gekro Lab — Decision Log

This log tracks architectural and technical decisions. Before implementing counter-intuitive requests, the agent will cross-reference this log and request an **Override** if a conflict is found.

| Date/Time | Decision | Rationale | Implications | Override Count |
|---|---|---|---|---|
| 2026-03-24 00:10 | Use "Technical SVG Schematics" for Lab Gallery | Physical photos are hard to maintain and can feel dated. SVG schematics reinforce the "Engineering Lab" aesthetic. | Consistency in visual storytelling across About/Gallery pages. | 0 |
| 2026-03-24 00:40 | Migrate Experiments to Content Collections | Sanity-based fetching proved fragile for core site features. Local Markdown files provide better build-time stability and SEO. | Local-first architecture; removed Sanity dependency for experiments. | 0 |
| 2026-03-24 01:10 | "Authoritative Engineering Lab" Persona | Shift site tone from "Personal Blog" to "Live Engineering Log." | Impacted all copy, footer heartbeat, and technical depth in posts. | 0 |
| 2026-03-24 01:15 | Continuous Issue & Decision Logging | To prevent development cycles and regressions per user request. | Agent must read logs before every planning/execution phase. | 0 |
| 2026-03-24 02:00 | Gekro "Deep Dive" Content Standard | Establish a rigid structure for technical posts to ensure authority, depth, and persona consistency. | Rewriting 8 posts; impacts all future content structure and voice. | 0 |
| 2026-03-24 04:45 | Centralized Post-Fetching via `posts.ts` | Duplicated Sanity+local merge pattern across 4 pages was a maintenance hazard. Single `getAllPosts()` utility eliminates this. | All pages/API now source posts through one path. Must update `posts.ts` for any schema changes. | 0 |
| 2026-03-24 04:45 | Env-Var-Only Secrets | All API keys, analytics IDs, and service keys must come from env vars (`import.meta.env` / `process.env`). No hardcoded fallbacks in source. | Added `.env.example` as the single source of truth for required vars. | 2 |
| 2026-03-24 13:45 | Content Integrity Standard | All blog/experiment content must be cross-referenced for hardware consistency (Pi count, specs), provider naming consistency, and factual accuracy. Unsubstantiated claims must be softened or evidenced. Code examples must be complete (no `...` or `your_module`). | Applied to all 11 content files. Standardized on 3× Pi 5 16GB + M.2 256GB across all articles. | 0 |
| 2026-03-28 16:35 | Deep Dive Content Standard V2 | Adopt V2 structure (Hook, Architecture, Build, Tradeoffs, Where This Goes) and "Rohit" persona. Rigidly enforce first-person, no clichés, and 6-8 min reading time. | Mandatory adherence for all future technical posts. Updated SKILL.md and template. | 0 |
| 2026-03-30 06:43 | Hardcoded GTM Tag | User explicitly overrode Env-Var-Only Secrets for Google Tag Manager to resolve analytics issue directly in the view. | Hardcoded GTM ID in BaseLayout.astro. Removed `PUBLIC_GTM_ID` from `.env.local` and `.env.example`. | 0 |
| 2026-03-30 06:53 | Hardcoded Cloudflare Tag | User explicitly overrode Env-Var-Only Secrets for Cloudflare Web Analytics token. | Hardcoded Cloudflare beacon snippet in BaseLayout.astro. | 0 |
| 2026-03-31 23:30 | Localized Light Reader Mode | The reading pages (Blog/Experiments) required a light mode toggle (like deepwiki) without changing the global dark mode. Implemented View Transitions API and scoped CSS variables. Later escalated light theme binding to `document.body` to resolve global fixed header contrast issues. | Requires a cleanup script bound to `astro:after-swap` to strip the light theme from the body if navigating back to the homepage. | 0 |
| 2026-04-01 00:10 | Performance Overhaul vs Aesthetics | Addressed 34-second TBT via `requestIdleCallback` for WebGL and GSAP, but actively declined removing the GSAP Hero text fade-in despite Lighthouse Element Render Delay penalty. | The premium "wow" factor of the entrance animation overrides achieving a literal 100/100 LCP score. | 1 |
| 2026-04-11 | Portable Blog Post Generator + `blog-drafts/` convention | A single self-contained `BLOG_POST_GENERATOR.md` can be dropped into any active repo so the AI agent scans the codebase in-place and drafts a Deep Dive that matches gekro's current voice — avoiding the friction of cloning the target repo into gekro. The only valid draft target is `./blog-drafts/YYYY-MM-DD-slug.md` at the root of the working repo (gitignored on first run), never inside any publishable content folder. Canonical source lives at `.gekro/docs/BLOG_POST_GENERATOR.md`; downstream copies are re-synced manually. | All future Deep Dives authored outside gekro flow through this generator. Drafts never hit `apps/web/src/content/blog/` until the user manually moves them after review. Generator self-updates by fetching the 2 most recent live posts as style reference before every run, and proposes amendments to itself in the gekro canonical after each session. | 0 |
| 2026-04-11 | Deep Dive Standard V2.1 — Section V split | Section V of the Deep Dive structure now has two valid headings instead of one: `## What I Learned` for retrospective / hands-on posts (default when writing alongside real code), and `## Where This Goes` for forward-looking / speculative / framing posts (e.g. green tech predictions, exploratory research). Codifies the pattern already present in `pi5-mcp-orchestrator.md` (`What I Learned`) vs `ai-codes-like-genius.md` (`Where This Goes`). | Updated `deep-dive-standard.md` to V2.1 and `BLOG_POST_GENERATOR.md` Section 5.V in lockstep. No retroactive rewrites of existing posts required — both forms are now canonical. | 0 |
| 2026-04-11 | Deep Dive Standard V2.2 — Length cap relaxed to soft target | The 6–8 minute reading-time target is now a default, not a hard ceiling. Content-rich posts with layered architectures, multi-stage builds, or multiple distinct war stories may run longer when the subject genuinely demands it. Padding remains forbidden — before exceeding the target, the writer must audit for filler (restated points, ceremonial transitions, hedging) and cut it. Overshooting is explicitly framed as a rare exception, not a license to sprawl. | Updated `deep-dive-standard.md` Section 2 to V2.2 and `BLOG_POST_GENERATOR.md` Section 5 "Length discipline" subsection + Section 6 frontmatter comment + Section 11 Example D phrasing. No retroactive rewrites of existing posts — all 11 current posts already fall within 5–8 minutes. | 0 |
| 2026-04-15 | Replace Beehiiv iframe embed with native dark-styled form | Beehiiv's iframe is cross-origin — their internal CSS forces a white background that cannot be overridden from outside (same-origin policy). The hardcoded `height="339"` also clipped the subscribe button. A native form POSTing to the same Beehiiv subscribe endpoint (`subscribe-forms.beehiiv.com/{id}/submissions`) via `fetch()` with `mode: 'no-cors'` gives total visual control and identical functionality. No-cors means an opaque response; success is shown optimistically (any non-thrown response = success; only caught network errors trigger an error state). | `NewsletterEmbed.astro` no longer contains an `<iframe>`. The `PUBLIC_BEEHIIV_PUBLICATION_ID` env var is still required. `data-wired` guard prevents listener stacking on `astro:after-swap`. Dev fallback (no publication ID) shows a local-only success message. | 0 |
| 2026-04-17 | Code-review sweep: 5 targeted refactors | Full code-review pass (with a verification sub-agent triage to weed out hallucinated findings) identified 5 real improvements worth landing together: (1) escape user input in LabTerminal command echo; (2) extract WORDS_PER_MINUTE constant + delete dead duplicate of `calculateReadingTime` from posts.ts; (3) consolidate Pagefind UI CSS variables from SearchWidget + SearchModal into a scoped block in global.css; (4) replace `any`-typed Props in BlogLayout/ExperimentLayout with `FormattedPost` and a new `ExperimentMeta` interface; (5) wire GiscusComments to a `gekro:theme-change` CustomEvent from ThemeToggle so comment theme tracks in-page toggle. Dropped findings: the agent flagged several false positives (duplicate copy handlers, missing startViewTransition guard, setInterval accumulation, RAF listener leaks) that ground-truth verification disproved — left unchanged. | Review cadence: when touching multiple components, spend a review pass catching small issues together rather than one-off follow-ups. | 0 |
| 2026-04-16 | Canonical URL convention: no trailing slash (except root) | Google Search Console reported "Alternate page with proper canonical tag" for every page because `Astro.url.href` produces `/blog/` while Google crawls `/blog`. Adopting no-trailing-slash as the site-wide canonical convention eliminates the ambiguity. Three enforcement points: `SEOHead.astro` (meta tag), `astro.config.mjs` sitemap serializer (XML), `public/_redirects` (Cloudflare edge 301). Root `/` is the only exception — it has no trailing segment to strip. | All canonical `<link>` tags now read `https://gekro.com/blog` (not `/blog/`). Sitemap entries match. Cloudflare 301s absorb stray trailing-slash requests before they hit SSG-served pages. | 0 |
| 2026-04-17 | **REVERSED** 2026-04-16: canonical = trailing-slash (natural form) | The no-trailing-slash strategy was incompatible with Astro's directory-format build output (`dist/blog/slug/index.html`) because Cloudflare Pages auto-redirects non-slash URLs back to the slash form, looping against our strip-trailing-slash `_redirects` rule. Root cause of the loop was a stack-up of three well-intentioned pieces (SEOHead normalisation + sitemap serializer + _redirects 301) that together fought CF Pages' built-in directory canonicalisation. Also: the original GSC signal ("Alternate page with proper canonical tag") was Google *correctly* respecting the canonical tag, not an indexing failure — so the 2026-04-16 change was solving a non-problem. | Canonical, sitemap, and `_redirects` all reverted to the natural trailing-slash form. If the GSC "Alternate" signal genuinely becomes a problem, the correct fix is either `trailingSlash: 'never'` + `build.format: 'file'` in astro.config (so output is `slug.html` instead of `slug/index.html`), or just accept Google's canonical-respecting behaviour. | 0 |
| 2026-04-17 | SEO + GEO sweep — no content edits, no new deps | Full structured-data + discoverability pass driven by "rank higher on Google/Bing/LLMs". Landed: (1) expanded SEOHead with article:* OG tags, twitter:creator, per-post ogImage plumbing, `<link rel=alternate>` for /llms-full.txt and /rss.xml; (2) expanded BlogPosting/TechArticle JSON-LD with image, dateModified, wordCount, keywords, articleSection, mainEntityOfPage; (3) BreadcrumbList on posts/experiments/topics/blog-index/experiments-index; (4) WebSite (with SearchAction) + Organization + Person global schema via `GlobalSchema.astro` on homepage; ProfilePage + Person on /about anchoring `/about/#person` @id; CollectionPage + ItemList on /blog, /experiments, /topics/[slug]; (5) sitemap `serialize()` hook emitting lastmod from frontmatter dates + per-URL-pattern priority/changefreq; (6) `/llms-full.txt` endpoint — flat plain-text manifest of every post+experiment (aiSummary preferred), plus regenerated public/llms.txt and expanded robots.txt allowlist (OAI-SearchBot, Applebot-Extended, CCBot, etc.); (7) per-post OG image endpoints at `/og/blog/[slug].png` and `/og/experiments/[slug].png` rendered from SVG via sharp — zero new deps; (8) custom /404 page with recent-posts fallback. **Zero article-content edits** per user directive "do not touch article content"; audit of title/description/aiSummary lengths written to `.gekro/logs/seo-audit-2026-04-17.md` as a report for future Cross-Sync pass. | Every page ships with image-ready, AI-parseable structured data. GEO crawlers (GPTBot, ClaudeBot, Perplexity, etc.) now have a single canonical manifest at `/llms-full.txt` with aiSummary per entry. Build is green (43 pages, up from 42). Post-deploy: verify via curl that `/llms-full.txt`, `/og/blog/<slug>.png`, and sitemap lastmod render on gekro.com. | 0 |
| 2026-04-15 | Visual "wow" features — no new dependencies | Added 5 polish features to transform the site from "well-built blog" to "engineer who sweats details": (1) Particle cursor repulsion in Three.js hero (`Vector3.unproject()` + inverse-square force, desktop only); (2) 3D perspective tilt on PostCard and ExperimentCard (±8°, GPU-composited via `will-change: transform`, reduced-motion gated); (3) CursorFollower — 12px accent dot with lerp trailing, morphs to 40px ring on interactive elements, `pointer:fine` + `min-width:768px` only; (4) SearchModal — Cmd+K/Ctrl+K Pagefind overlay, lazy-loaded on first open; (5) Discoverability hints — Footer keyboard shortcut hint at 30% opacity, ThemeToggle one-time pulse animation (sessionStorage-gated). Zero new npm dependencies. Mobile impact: zero (all features CSS/JS-gated to desktop pointer devices). | `CursorFollower.astro` and `SearchModal.astro` are new global components mounted in `BaseLayout.astro` outside `#swup`, persisting across View Transitions. `ExperimentCard.astro` now has full JS block (was previously no-JS). `PostCard.astro` lost `hover:translate-y-[-4px]` Tailwind class (replaced by JS transform). | 0 |
| 2026-04-19 | Introduce `/apps` section — single-session client-only mini-tools | gekro is positioning as an AI-engineer's public lab + portfolio front. The blog is the lab notebook; apps are the working bench. Every app must be (a) something Rohit personally uses, (b) shareable as static client-side code with no backend or user data, (c) accompanied by a methodology blog post that two-way-links to the app. First app: LLM cost calculator with three modes — API cost / local hardware TCO (AI Hat 2 vs RTX 5060 vs cloud) / side-by-side. Architecture mirrors the blog pattern exactly: new `apps` content collection (Zod schema in `content/config.ts`), `pages/apps/index.astro` grouped by `category` frontmatter, `pages/apps/[slug].astro` dynamic route, `AppLayout.astro` extending `BaseLayout`. Hard guardrails to prevent overengineering: static-only (no APIs/edge functions/DB), localStorage for preferences only (never user data), no new npm deps until 3+ apps demand the same one, one file per app until it grows past ~400 lines, no premature shared "calculator framework". Hardware-comparison apps use a curated benchmark JSON (`{ model, hardware, quantization, tokens_per_sec, watts }`) sourced from published numbers + Rohit's own runs — never predict throughput from raw specs. Pricing data lives in a single `pricing.json` per app with a "last verified" date shown in the UI. Cap on cadence: one new app per month until traffic data justifies more. | New collection + routes + layout to ship, but the pattern is fixed and reusable for every subsequent app. Apps are open-sourced as part of the repo — companion to the blog, not a separate codebase. Failure mode to watch: data freshness drift (model prices change monthly) and feature creep within a single app (resist export/share/PDF until usage justifies). | 0 |
| 2026-04-19 | `/apps` scope expanded to platform — 20+ apps planned | User confirmed roadmap of 20+ apps. Scale inverts the earlier "no shared utils until 3 apps" guardrail: shared abstractions are now justified upfront by real cross-cutting requirements, not speculation. Locked-in cross-cutting features that ship from day one: (1) **CSV export** on every app — `components/apps/shared/csv.ts` exposes `exportRows(rows, filename)`; (2) **URL-state sync** on every app — `shared/url-state.ts` encodes inputs in querystring so scenarios are bookmark-able and link-shareable without auth (bigger democratization win than CSV alone); (3) **Shared UI primitives** — `NumberInput`, `Select`, `Tabs`, `ResultCard`, `CopyButton`, `ExportButton`, `ResetButton` — all Tailwind-token-based, no custom CSS; (4) **AppShell** with toolbar (CSV + Copy + Share-link + Reset) and "last verified" stamp; (5) **Landing page** with vanilla-JS search + category filter chips (no Fuse.js, no Pagefind for /apps — overkill for 20–50 items). Per-app UX standard locked: sensible defaults so results render instantly (no blank state), live recalc on input (no submit button), mobile-first numeric inputs, methodology link to companion blog post. **Categories taxonomy v1 (locked, additive only):** `ai`, `infra`, `ev`, `trading`, `dev`, `finance`. Every app declares one. **Cadence still capped:** 1 app/month until traffic data justifies more. The full canonical spec lives in `.gekro/docs/apps-platform-standard.md`. | All shared utilities ship with the platform scaffold (not lazily added later). Adding an app becomes: 1 markdown file + 1 island component + (optional) 1 data file. Resist any expansion to the shared layer beyond the locked list — new shared primitives need an explicit decision-log entry. | 0 |
| 2026-04-19 | Phase 1 data approved — LLM Cost Calculator (App #1) | User approved researched benchmark + pricing data. Key decisions confirmed: (1) Pi hardware = Pi AI HAT+ 2 (Hailo-10H, 40 TOPS) scoped to 1.5–3B models only — Hailo-8/8L cannot run 7B+ models; (2) OpenAI = GPT-5.4 + GPT-5.4 mini (current, not deprecated Aug-2025 GPT-5); (3) Null benchmarks show "No published data" greyed out — no estimation. Approved tagline: *"Free tools I built for myself. Sourced data. No login."* — baked into standard §1. Parking lot additions: App #2 Text-to-Markdown Visualizer (dev category, dep decision needed for Markdown parser), App #3 Voice Recorder + Transcript Generator (ai category, dep decision needed for Whisper.js vs Web Speech API). Full parking lot tracked in `.gekro/docs/apps-parking-lot.md`. | Build proceeds: Phase 2 scaffold + Phase 3 Calculator in one pass. | 0 |
| 2026-04-20 | 11 dev-tool mini-apps shipped — hand-rolled micro-faker, zero backend | Added apps: json-formatter, text-diff, jwt-decoder, password-generator, dummy-data-generator, code-snippet-png, color-toolkit, gradient-generator, image-compressor, csv-to-json, device-info. All client-side. Deps: diff, papaparse, browser-image-compression, html-to-image, shiki — all MIT/Apache. Micro-faker hand-rolled (~10 KB) instead of @faker-js/faker (1.7 MB). Each app follows `astro:after-swap`+`_xyzInit` guard pattern. | All 11 registered in [slug].astro CALCULATOR_MAP. Build clean at 73 pages. | 0 |
| 2026-04-21 | Apps catalogue rebalance: 9 new on-brand apps + Featured/Other split, no-delete rule | External audit found the apps section under-represented the "AI engineer's lab" positioning — 18/31 apps were generic dev utilities, only 3/31 were AI-engineer-specific. Two-pronged response. **Restructure:** apps page now sorts by category priority (`ai → trading → ev → infra → dev → finance` then `health → fun`), splitting into two visual sections — "Featured" (AI/infra/eng) and "Other Tools" (personal-utility apps Rohit built so he wouldn't need to visit other sites). When a specific filter chip is active, section headers hide so the grouping only shows in the default "all" view. **Hard rule logged in memory:** apps are NEVER deleted — URL preservation, SEO, link continuity matter; if an app feels off-brand, re-categorise to lower priority instead. Cadence cap (1 app/month) is now informal — apps ship as ideas arrive (e.g., QR code generator was built same day Rohit needed one). **9 new apps shipped, all featured-tier:** Tokenizer Visualizer, RAG Chunk Inspector, GPU VRAM Calculator, EV Charging Cost Calculator, Docker Compose Visualizer, JSON Schema → LLM Tool Definition, System Prompt Linter, Context Window Visualizer, systemd Unit File Generator. All zero-new-deps. All follow standard pattern (idempotency guard, app:reset/copy/export listeners, live recalc, prefilled examples). Catalogue now 40 apps total; AI category jumped from 3 → 9. | Memory updated with no-delete rule (`project_apps_initiative.md`) — overrides any future agent instinct to "trim" the catalogue. SEO note: apps still get per-slug OG images (`/og/apps/[slug].png`) auto-generated. Build clean at 84 pages, zero TS errors. Three rounds of testing landed: build verification, bug audit (1 real fix: gpu-vram-calculator NaN guard for custom params), UX polish (tokenizer + RAG inspector render caps for huge inputs, system-prompt-linter score interpretation legend). | 0 |
| 2026-04-20 | Full SEO/GEO sweep — 20+ targeted fixes, zero blog-content edits | Comprehensive SEO pass to maximize traffic and AI-crawler discoverability. Changes: (1) SEOHead: added `noindex` prop + `<meta name="robots">` tag (default: index,follow); removed redundant `<meta name="title">`; improved og:image:alt and twitter:image:alt; improved default site description. (2) BaseLayout: `noindex` prop threaded through to SEOHead. (3) 404.astro: `noindex={true}` wired — was previously missing despite the comment saying it existed. (4) Experiment frontmatter: fixed 3 `startDate` values from human-readable "Month Year" to ISO 8601 (YYYY-MM-DD) — was causing invalid JSON-LD `datePublished`. ExperimentLayout now formats date for display using `toLocaleDateString`. (5) JSON-LD author @id: added `@id: "https://gekro.com/about/#person"` to all 3 layouts (Blog, Experiment, App) — enables entity resolution by Google's Knowledge Graph. (6) JSON-LD publisher logo: upgraded from `favicon.svg` to `og/default.png` (1200×630) + added `@id: "https://gekro.com/#organization"` for entity linking. (7) BlogLayout dateModified: uses `updatedAt` frontmatter field when present (falls back to publishedAt); `updatedAt` added as optional field to blog Zod schema. (8) RSS: fixed link trailing slash; added `<author>` per item + `<managingEditor>` + `<webMaster>` to channel. (9) Topics breadcrumb: fixed position-2 item from `/blog/` to `/topics/`. (10) Blog index description: fixed typo "systems build" → rewrote to keyword-rich 155-char description. (11) Meta descriptions: rewrote all static page descriptions (experiments index, about, now, contact, slides, homepage, apps index, topics) to include target keywords and person name. (12) astro.config.mjs: added `filter()` to exclude `/og/` and `/api/` from sitemap; added apps to `scanCollection`; added `/apps/` to index tier (priority 0.9) and individual apps to 0.7 tier. (13) Per-app OG images: new `/og/apps/[slug].png.ts` endpoint mirrors blog/experiment pattern — each app now gets a branded preview card. AppLayout wired to use `ogImage=/og/apps/${slug}.png`. (14) llms.txt: added `/now`, `/slides`, `/apps` navigation entries; expanded topic cluster list; updated date. | Zero blog-content edits (per user directive). All changes land in layout/config/static/frontmatter-of-apps files. Post-deploy verify: (a) Google Rich Results Test on a blog post — check dateModified ≠ datePublished after first `updatedAt` use; (b) curl /og/apps/llm-cost-calculator.png — confirm 200 PNG; (c) curl /rss.xml — confirm `<author>` elements present; (d) GSC sitemap resubmit. | 0 |
| 2026-04-24 | Two new trading apps shipped: Options P&L Calculator + Drawdown & Sharpe Calculator | Both are zero-dep, client-side only, trading category. Options P&L: Black-Scholes Greeks via Abramowitz-Stegun CDF polynomial, expiry P&L for 6 strategies (long call/put, covered call, cash-secured put, bull call spread, bear put spread), hero stats, P&L table at ±50% range. Drawdown calculator: paste return series → Sharpe/Sortino/MaxDD/Calmar/WinRate/AvgWinLoss, CSS equity curve, top-5 drawdown episodes. Both follow standard pattern: `_xyzInit` idempotency guard, `astro:after-swap` re-registration, all 3 AppShell events wired. CALCULATOR_MAP not updated (per hard rule not to edit [slug].astro) — apps render content/methodology correctly with "Calculator coming soon" fallback until map entry is added. | Adds 2 apps to trading category (alongside position-sizer and tesla-trip-calculator). Trading is now the most comprehensive category on the site. | 0 |
| 2026-04-19 | Open-source licensing: MIT (code) + CC BY-NC 4.0 (content) + per-app override | Repo was previously unlicensed (default "all rights reserved" — technically blocking all reuse). Adopting dual-license pattern: (1) **Root `LICENSE` (MIT)** — covers all code in the repo, including all apps. Standard, attribution-required (must include copyright + license text), allows commercial reuse. (2) **Root `LICENSE-CONTENT` (CC BY-NC 4.0)** — covers blog posts, app copy, methodology text. Requires attribution, blocks commercial reuse (deters SEO-farm scraping). (3) **Per-app `license` frontmatter field** in `apps` collection schema, defaults to `MIT` — allows future override on a per-app basis if a specific app ships data/dependencies that require different terms. (4) **`AttributionFooter.astro`** rendered on every app page reading the frontmatter license value: "© 2026 Rohit Burani · {license} · Built at gekro.com · View source on GitHub". Visible attribution backs up the legal license with social pressure. (5) **`README.md` updated** to state both licenses, link to LICENSE files, and explain the per-app override convention. | Repo state changes from "unlicensed/all rights reserved" to "dual-licensed open source." Standard pattern for code+content sites. Per-app override exists but should be rare — overuse fragments the licensing story and confuses contributors. Any per-app license that isn't MIT requires its license text added to a `LICENSES/` folder at root. | 0 |
| 2026-06-01 | **REVERSES 2026-05-31** + stack template made exploratory | Owner feedback after seeing it live: the referral was effectively invisible (he looked under the "Try it" CTA + footer and could not find it, because the CTA pointed at homepage and the only referral was a subtle in-body link). Also: the entry "looked very mechanical" and not user-friendly. | **Referral:** `StackVerdict` now renders an action row - primary `app-btn-primary` "Try {tool}" carries `referralLink` (unlabelled, so still discreet); secondary `app-btn-secondary` "Visit site" points at `homepage`; in-body link + mandatory muted footer retained. **Template (applies to all entries):** pros/cons now render as side-by-side tinted cards in a 2-col grid (StackPros/StackCons + StackLayout grid wrapper); StackMeta restyled as a spec-sheet of tiles; StackCard gained a category·price eyebrow. All within locked design tokens, reusing app-btn classes. stack-standard.md §2/§4/§6 updated. Build green at 117 pages. | 1 |
| 2026-05-31 | Stack referral handling: top CTA = homepage, referral subtle in-body + mandatory muted footer | Per Rohit's branding ("pro-level AI engineer; the referral must not overshadow the site"), the prominent top-of-page link must stay editorial. Previously `StackVerdict` set the "Try it ->" CTA to `referralLink ?? homepage`, putting the monetized link in the most prominent slot. | `StackVerdict.astro` now always links the CTA to `homepage` (referralLink prop removed); `StackLayout.astro` no longer passes referralLink to it. A `referralLink` now only (a) triggers the `<StackReferralFooter>` muted disclosure (always kept - non-negotiable) and (b) is hand-placed as one subtle in-body prose link. Documented in stack-standard.md §2/§4/§6. First entry built under this rule: `wispr-flow.md` (draft, not committed/pushed). Reversible per-entry later if a referral CTA is ever wanted. | 0 |
| 2026-06-02 | Translator app — in-browser neural MT (M2M-100) for EN/ES/HI | Rohit wanted a private, Google-Translate-style translator for English, Spanish, and Hindi (all directions, interchangeable). Chosen engine: **Meta M2M-100 418M** (`Xenova/m2m100_418M`) running client-side via transformers.js + ONNX Runtime Web. M2M-100 is *many-to-many*, so ES↔HI translates directly (no English pivot) — the deciding factor over Helsinki opus-mt pairwise models (which lack a direct es-hi pair) and over keeping it API-backed (defeats the privacy goal). transformers.js is dynamically imported from the jsDelivr CDN at runtime (same pattern as rag-eval-toolkit's semantic mode) so **no npm dependency is added** to the repo. Model (~250 MB quantized) downloads once with a streamed progress bar, then caches in browser Cache Storage and runs fully offline. Live debounced translation with stale-result guarding, source auto-detect (lightweight 3-language heuristic: Devanagari→Hindi, else ES-vs-EN stopword/accent scoring), language swap, browser TTS, copy/export. Category `ai`. Honest limitations surfaced in the methodology (download size, three languages only, quality below the largest cloud models, heuristic detection). | New app `translator` (Calculator in private `gekro-apps`, content md + CALCULATOR_MAP entry in public `gekro`). Build clean at 121 pages. First app to run a full seq2seq model in-browser — reinforces the "AI without a server" thesis. Not runtime-tested end-to-end in CI because the 250 MB model download is impractical in the build sandbox; API usage matches official transformers.js docs and the working rag-eval-toolkit CDN-import pattern. Recommend a manual smoke test post-deploy. | 0 |
| 2026-05-07 | App #51 pricing pipeline hardening — 3-layer bug fixes | Three-round code review of the hyperscaler pricing pipeline after Azure/Vertex expansion. **Fix 1 (AWS tier guard):** `fetch-aws.mjs` now checks `attrs.feature` before the usagetype filter — any feature string other than `"On-demand Inference"` is skipped. Fixes Mistral Large 3 returning $4.00/1M (Latency Optimized Inference) instead of the correct $0.50/1M (On-demand Inference); without this, the dedupe-first-wins rule in `update-pricing.mjs` happened to pick the premium tier first. **Fix 2 (Azure dead code):** Removed `tryNormalize`, `matchByVendorSku`, `require_normalize` functions from `fetch-azure.mjs` — they were never called from the live `fetchAzurePricing()` path and `require_normalize()` contained a broken synchronous-`import()` pattern. Also removed the orphaned `resolveCanonical` import. **Fix 3 (Azure multiplier):** Two sequential `if` statements for unit conversion meant `'1,000,000'.includes('1,000')` would set `multiplier = 1000` after a correct `multiplier = 1`, producing a 1000× wrong result for any per-million-unit SKU. Changed second `if` to `else if`. **Fix 4 (event listener lifecycle):** `Calculator.astro` (App #51) now uses `AbortController` on the three `document.addEventListener('app:*')` calls, with `astro:before-swap` as the abort trigger — prevents stale handlers from accumulating as users navigate between apps, each of which dispatches the same `app:copy/reset/export` events. **Fix 5 (CSV quoting):** `app:copy` handler now quotes `name`/`vendor` fields (was missing, unlike `app:export`). | Pipeline is now production-hardened. Known remaining gap: GCP Vertex SKU patterns in normalize.mjs are educated guesses; they'll either match on the first workflow run or need refinement via `--discover` output. | 0 |
| 2026-06-02 | Full code-review + consistency sweep (site + 69 apps) | Owner requested a vetting pass. Ran 4 parallel read-only reviewers (web core / apps a–l / apps m–z / security), then ground-truth-verified every finding before fixing (prior sweeps produced hallucinated findings). Mechanical listener-scoping fixes were delegated to 2 editing subagents partitioned by file (no overlap); security + logic fixes done by hand. | 47 app files + `topics/[topic].astro` fixed across both repos. Lesson reinforced: subagent "harmless" classifications are NOT authoritative — a deterministic verification script (per-listener signal check) is the gate, not reviewer confidence; it caught 5 real listener leaks the reviewers had waved through. Build clean at 121 pages. | 0 |
| 2026-06-19 | News personalization via a repo-local interest profile, refreshed weekly from Notion by a decoupled cloud routine (NOT yet implemented — design of record) | The daily /news briefing currently selects stories against a STATIC PREFER/AVOID list hardcoded in `generate-briefing.mjs` (the `CURATION_PROMPT`, ~lines 82-108). Owner wants the selection biased toward HIS actual, evolving AI/software interests so the briefing reflects what he is working on and writing about, rather than generic AI-engineer taste. Volume of AI news is the problem being solved: a personalized filter is the value. Two hard constraints shaped the design: (1) the morning briefing must NEVER gain a runtime dependency on Notion or on the owner's machine being on — it has to keep running unattended with only `ANTHROPIC_API_KEY`, fail-closed; (2) gekro is a PUBLIC repo and /news is a PUBLIC, neutral, third-person wire service — nothing personal, employer-specific (e.g. Southwest), client, financial-account, or otherwise sensitive may ever reach the profile or the site. | **Architecture (design of record, build later):** (a) A new VERSIONED artifact in the public repo — `scripts/news/interests.json` (shape ~ `{ updatedAt, source, topics[], emphasis[], avoid[], notes }`) — is the single source of personalization. Because it is committed to a public repo it is world-readable, which is the guardrail: only publishable topics can ever live in it. (b) `generate-briefing.mjs` reads this file LOCALLY at briefing time and injects a `READER FOCUS:` block into the selection criteria of the existing prompt (single new touch point near the `userPrompt` build, ~line 238). If the file is absent or malformed, the script FALLS BACK to today's static PREFER/AVOID list — the briefing is never broken by this feature; it is purely additive. (c) A SEPARATE weekly cloud routine — CONFIRMED: a scheduled Claude agent (`/schedule`), running in the cloud so the owner's computer need not be on — reads the Notion knowledge base, INTERPRETS the whole KB, then DISTILLS it to public AI/software topics only — applying an allowlist (AI/ML/infra/tooling/software topics Rohit has written about or is exploring) and a hard denylist (employer/clients, trading account specifics, personal life, anything identifying or sensitive) — and writes the refreshed `interests.json`. CONFIRMED update gate: the routine AUTO-MERGES to `main` (fully hands-off, no PR review) BUT only after a mandatory sensitivity sanity-check it runs on its own output — a hard self-check that the distilled profile contains nothing personal/employer/client/financial/identifying. The check is fail-closed: if the routine cannot certify the profile is clean, it ABORTS and writes nothing, leaving the previous profile in place. Stale-but-clean always beats fresh-but-unvetted on a public site. (d) The car/TTS consumption layer and the RSS feed are UNCHANGED and unaware of any of this. **Personalize SELECTION, not ADDRESS:** the public post stays neutral third-person (no "for you, Rohit"); any personal framing belongs only in the private TTS layer. This does NOT override the neutral-voice content standard (we change which stories are picked, not the voice) — logged here for traceability, not as an Override. **Open item before build:** Notion MCP may be unavailable in headless/cron runs (interactive auth) — the weekly scheduled Claude routine likely needs a non-interactive Notion integration token + a repo-write token wired into the routine's environment. (Runner = Claude cloud routine and gate = auto-merge-after-sanity-check were both confirmed by owner 2026-06-19.) | 0 |
| 2026-06-21 | Intensive multi-round code review (whole repo) + fixes | Owner asked for 5-6 full word-to-word review passes across the repo, fixing along the way. Ran 5 parallel review agents by lens (web core / apps a-m / apps n-z / security / docs+comments+README), then ground-truth-verified every high/med finding myself before fixing (reviewers were accurate on all spot-checks — options-pnl, tesla-charge, nginx, systemd, cron all confirmed real). Fixes applied via direct edits for the math/config-critical bugs + 5 parallel fixer agents (partitioned by file, no overlap) for the mechanical/escaping/docs fixes. | ~30 app correctness/security bugs fixed (gekro-apps@2c163da) + 7 web-app/comment fixes + a full documentation-accuracy sweep. Headline correctness fixes: options-pnl put delta/theta (verified via put-call parity), tesla-charge charging-loss double-cancel, nginx SSL-cert omitted under https-redirect, systemd invalid inline comments + Requires anti-pattern, cron DOM/DOW AND-vs-OR. Security: jwt/password/dice/device innerHTML self-XSS escaping, JsonLD `</script>` escaping (reachable via auto-published news titles). Docs: Astro 4->6 everywhere, app counts ->72, "submodule"->cloned-at-build, content/config.ts->content.config.ts, llms.txt content license CC BY->CC BY-NC, decision/issue counts refreshed, context maps (architecture/pages/components) brought current. Build clean 133 pages; listener-scoping re-verified 0 regressions. **Deliberately left as-is (with rationale):** rag-eval precision@K (hits/retrieved is a defensible "precision of returned set" definition); CJK/emoji token counting in tokenizer/word-counter (apps already warn about non-Latin; proper segmentation is a feature, deferred); GCP billing key passed in URL query in scripts/pricing (build-time only, key is read-only/restricted — flagged to owner to add key restriction); Mermaid securityLevel:'loose' (trusted author markdown only). | 0 |
| 2026-06-21 | CI typecheck made non-blocking + all 89 `astro check` errors fixed | The CI "Type check" step (`astro check`) was BLOCKING and failing on 89 pre-existing type errors, so every daily news auto-commit push re-ran CI, failed, and emailed a pipeline-failure notice. Production was never affected (Cloudflare deploys via `astro build`, which does not run a typecheck). Root causes were concentrated: base tsconfig `moduleResolution: "node"` couldn't resolve subpath exports (`astro/loaders`, `shiki/bundle/web`); LabTerminal's web-component class fields were declared `= null` (TS inferred `null`/`never`, cascading into 40 errors); `FormattedPost` lacked `faq`/`howto`; StackCard Props used `slug` while the code read `id`; LabGallery had a malformed double-`---` frontmatter (unused component, so the build never caught it); plus scattered implicit-any / index-signature / Motion-One-overload issues. | (1) `ci.yml`: the typecheck step is now `continue-on-error: true` (same as Lint) so daily content automation can no longer fail CI or email - the permanent fix for the emails. (2) Fixed all 89 errors so `astro check` passes legitimately (0 errors / 0 warnings; 152 unused-var hints remain, non-failing): web `tsconfig` `moduleResolution: "bundler"`; typed LabTerminal fields + `querySelector<T>` + handler params; added `faq?`/`howto?` to `FormattedPost`; StackCard `slug`->`id`; repaired LabGallery frontmatter; ambient `declare module 'https://*'` (`cdn-modules.d.ts`) for the transformers.js CDN imports; `as any` casts on the 3 Motion One `animate()` calls (easing-tuple widening); typed the news source-map tuple; apps-private fixes in gekro-apps@c92009c. Build clean at 133 pages. | 0 |
| 2026-06-21 | News GEO fix (articleBody in JSON-LD) + detail/index UX | Owner reported AI assistants (Grok) reading "only the title" of news pages and news not driving traffic. Diagnosed against the live site: robots open to all AI bots, all 19 articles in the sitemap, canonical + index,follow correct, NewsArticle JSON-LD present - BUT the schema had only `headline` + 1-sentence `description` and NO `articleBody`, so engines that read structured data first got essentially just the title. Article bodies are also thin (~319 words/briefing), which limits organic search regardless of polish. | (1) Enriched the NewsArticle JSON-LD with `articleBody` (full markdown-stripped text), `wordCount`, `articleSection`, `inLanguage`, and `citation` (the source list as CreativeWork[]) - so AI engines/crawlers get the whole briefing + its sources, not just the headline. (2) UX: added a top-of-article "The gist" TL;DR callout (scannable for humans, clean summary block for SEO/AI), a meta row (reading time + source count), and a one-line orientation dek under the /news masthead. Build clean at 133 pages; verified articleBody/citation/TL;DR render in static HTML. Honest caveat recorded: thin daily briefings compete poorly with major outlets for search traffic - news is best treated as a GEO/citation + freshness play, which these changes optimise for. | 0 |
| 2026-06-22 | News editorial redesign (template for site-wide pass) | Owner critique: the site reads "novice", too many tiny mono-uppercase labels + wasted whitespace; wants an expert, human-friendly, space-efficient editorial/magazine aesthetic across the whole site. Two concrete examples on the news detail page: the "News Feed" back-link was a tiny 11px tap target, and Sources were a tall vertical list wasting a column of height. | Reworked the news detail + index as the editorial template: (1) back-link -> real pill button (border + padding + hover, proper tap target); (2) Sources -> compact horizontal chip row (reuses the index card pattern) - big vertical-space win; (3) editorial header: `font-display` (Syne) headline at 2-2.75rem, a readable standfirst dek (the summary), and a hairline byline rule (date · read time · source count) replacing the boxy "gist"; (4) body type bumped to 16-17px / 1.8 leading; (5) index masthead to font-display. Verified via screenshot (desktop). This is the agreed system (editorial/magazine, chosen by owner) to propagate next to blog / apps / stack / experiments + global header/footer. | 0 |
| 2026-06-22 | News personalization (interests.json) IMPLEMENTED + citation-integrity + freshness hardening | Implements the 2026-06-19 design of record, and fixes two live defects found while verifying freshness for the launch article: a multi-day story re-reported every morning (no dedup) and model-fabricated citation URLs (the model was never handed the real feed URLs). Owner's bar was explicit: "no learned listener can call BS." | **interests.json** (`scripts/news/interests.json`, public, AI/software topics only) is read by `generate-briefing.mjs` into a READER FOCUS block that biases SELECTION only (never the neutral voice); missing/malformed → static-prompt fallback, so it is purely additive and cannot break the daily run. **Citation integrity:** candidate feed URLs are now passed to the model, which must cite only those verbatim; the validation gate rejects (fail-closed) any `sourceUrl` not in the supplied allow-list (normalised origin+path) — fabricated citations can no longer publish. **Freshness:** the last 6 briefing titles are fed back so multi-day sagas are not re-reported. **Sources:** added MarkTechPost + The Decoder (both verified 200/RSS) to `generate-briefing.mjs` + `fetch-headlines.mjs` (kept in sync); MarkTechPost was previously a *confabulated* citation domain, now a real source. **Trade-off:** the strict allow-list can fail-close (skip a day) if the model disobeys the cite-verbatim instruction — failure mode is a skipped briefing, never a bad publish. NOT live-tested against the model here (no API key in this env); recommend one `workflow_dispatch` dry-run before relying on it. The weekly Notion→interests.json distill (cloud Claude routine) remains the external piece per 2026-06-19. | 0 |
| 2026-06-22 | Site-wide editorial/magazine redesign (propagated the news template) + deeper news briefings | Owner approved the 2026-06-22 news editorial template ("I like the updates") and directed it across the whole site ("update the thing system-wide"); separately asked that briefings be deeper-but-lean (3-4 paragraphs, not 2, and not a 500-900 word essay). | Propagated the editorial system to Blog / Experiments / Apps / Stack (index + detail), the global header, and the secondary pages (about / contact / now / slides / topics): `font-display` (Syne) headlines at restrained sizes (index 4xl-5xl, detail ~2.25-3.75rem) replacing the juvenile `text-7xl`/`text-8xl`; ONE accent mono kicker per masthead (replacing mono-uppercase label-spam); standfirst deks; hairline byline rules; tightened vertical rhythm (`py-32` -> `pt-28 sm:pt-32 pb-24`, `space-y-24` -> `space-y-14`); larger card titles (PostCard 24px, chips de-mono'd + capped at 2); header nav de-mono'd to sentence-case 13px + mobile menu set in Syne; removed the duplicate publish date on blog posts (PostHeader byline is now the single canonical date). All within locked design tokens (no new colors invented). **Deeper briefings:** `.claude/commands/news-briefing.md` now specifies 3-4 paragraphs / ~350-450 words (was "exactly 2 paragraphs"), neutral cited third-person voice + no-em-dash rule unchanged; affects future briefings only. Verified: clean build (142 pages, link-guard OK) + computed-style inspection on the live dev server confirmed Syne renders, responsive scaling (60px desktop -> 36px mobile), no mobile horizontal overflow, and exactly one date in the blog byline. Screenshots unavailable this session (preview viz backend errored) - verified via build + computed CSS instead. No `apps/web/src/content/**` article markdown touched. | 0 |
| 2026-06-23 | Automated apps test pipeline - vitest integrity (blocking) + Playwright e2e smoke (path-filtered) | Owner asked to thoroughly verify every app and "establish very thorough tests as part of the deployment pipeline" before announcing the apps on LinkedIn. The 72 client-side apps had ZERO automated runtime coverage - only `astro build` (compile) + one reading-time unit test. Nothing caught content<->CALCULATOR_MAP<->component drift (which silently ships a "Calculator coming soon" stub) or runtime/responsive breakage. | Two layers. (1) vitest `tests/apps-integrity.test.ts` (80 tests): asserts the 3-way content/map/component alignment + frontmatter validity (category enum + required fields). Fast, no browser, BLOCKING in ci.yml - safe because it only reads app files, so the daily news auto-commit cannot trip it. (2) Playwright `e2e/apps.smoke.spec.ts`: loads every `/apps/[slug]` at 375/768/1280, asserts the island mounts (not "coming soon"), renders content, throws no console/page errors (tight per-app allow-list for headless realities: no mic, CDN model loads, blocked network), and no horizontal overflow. Runs in a NEW `e2e.yml` workflow, path-filtered to app/content/shell changes + PRs, so the daily news commit does not pay the clone+build+Chromium cost. New dep: `@playwright/test` (devDependency) - per apps-standard #2 "no new npm deps" this entry is the required decision; it is test-only, justified by the #12 "LinkedIn-screenshot-worthy" quality bar needing automated enforcement across 72 apps (manual screenshotting does not scale). First run found 11 real horizontal-overflow bugs (see issue tracker 2026-06-23), all fixed; 72/72 now green. | 0 |
| 2026-06-23 | Hardcoded fallback for the Beehiiv subscribe-form ID (newsletter was dead on prod) | The live newsletter showed "temporarily unavailable" on every submit: `PUBLIC_BEEHIIV_PUBLICATION_ID` is set in local `.env.local` but was never added to the Cloudflare Pages build env, so the production build rendered an empty `data-publication-id` and the honest prod-misconfig branch fired for every visitor (visible bad look on the homepage). The form ID (`016ca01a-...`, a Beehiiv form UUID, endpoint confirmed valid - returns 403 not 404) is a PUBLIC client-side value that ships in page HTML on every render, NOT a secret. | `NewsletterEmbed.astro` now reads `import.meta.env.PUBLIC_BEEHIIV_PUBLICATION_ID \|\| '016ca01a-2686-4989-8f1e-feb399d46628'` - env-var-first, hardcoded public fallback so the form works regardless of the CF build env. Verified: build green (143 pages), homepage form ships the real ID. **Override of 2026-03-24 Env-Var-Only Secrets**, consistent with the GTM/GA4/CF-beacon hardcode overrides (2026-03-30) - same class of public, non-secret, client-side ID. Reversible: add the var to CF Pages env and the fallback is never reached. | 1 |
| 2026-07-05 | Wave 4 apps — 6 AI-engineering tools shipped in one batch + `global-clock` redesign | Owner granted full authority to build the 6-app Wave 4 blueprint (`.gekro/docs/apps-blueprint-wave4.md`) and ship straight to prod. Theme of the wave: **fixers, builders, and inspectors** — tools reached for mid-task, where habitual traffic comes from — to complement the catalog's existing depth in cost-calculators and visualizers. All 6 are category `ai`, zero-backend, client-only, and follow the locked platform contract (idempotency guard, `astro:after-swap` re-init, 3 AppShell events via one AbortController, meaningful first paint, clamped inputs, escaped innerHTML, design tokens only). Apps: **llm-json-repair** (forgiving recursive-descent JSON parser + zero-dep JSON-Schema validator), **llm-api-builder** (request → curl/Python/TS for Anthropic/OpenAI/Gemini; **no API-key input field ever** — env-var placeholders only), **embedding-playground** (real MiniLM embeddings in-browser via transformers.js CDN → cosine heatmap + top/bottom pairs + hand-rolled dual-PCA scatter), **gguf-inspector** (client-side GGUF v2/v3 header parse via `File.slice` + `DataView`, every read bounds-checked, reads only the header not the whole file), **finetune-dataset-auditor** (JSONL lint + token distribution + duplicate detection + editable FT cost, 50 MB cap), **rate-limit-planner** (RPM/TPM binding-limit + burst-drain queue math + tier suggestion over editable curated presets). `global-clock` reworked: 4 large featured clocks (Chicago, Mumbai, Santiago, London) with simplified analog hands (muted second hand replacing a jarring red one), other 8 cities as smaller tiles. | Catalog 72 → **78 apps**. Components in `gekro-apps` (9da652c, c8f03ba, ef22a54); content + wiring in public `gekro` (e1d0582, 2c1d6c5, 6e1d8d5). Verified: build clean (160 pages), vitest 86/86, Playwright e2e 156/156 (smoke + interaction/fuzz). Wave 4 queue cleared from parking-lot + memory backlog. New reusable pattern captured: **dual-PCA via the N×N Gram matrix + power iteration** is the cheap way to do a 2D projection when samples ≪ dimensions. `embedding-playground` got an e2e error allowlist entry (its CDN model fetch is blocked in headless). | 0 |
| 2026-07-06 | Build-time private-repo clone is a single point of failure — PAT rotation protocol | Reinforces the 2026-05-08 "clone-at-build-time, NOT a submodule" decision after it bit hard: both build PATs (`GITHUB_PAT` in CF Pages, `SUBMODULE_PAT` in Actions) expired the same day and silently froze production on its last good build while CI went red (full RCA in issue-tracker 2026-07-06). The clone-not-submodule architecture is still correct (CF Pages can't inject private-submodule auth), but it means **an expired read-only PAT takes the entire site's deploy pipeline down with a non-obvious symptom** (stale content, not an error page). | Protocol of record: (1) both secrets hold the **same** fine-grained PAT (Contents: Read-only on `drajb/gekro-apps`) and are **rotated together**; (2) set **max expiry** on the replacement and/or a calendar reminder ~11 months out; (3) first diagnostic when prod shows stale content = check the CF Pages build log for `remote: Invalid username or token` at the clone step. Documented in CLAUDE.md §8 (secrets note), the `gekro-apps` CLAUDE.md §5, and the dual-repo memory. Not an Override — this is additive hardening of an existing decision. | 0 |
