# Gekro Lab — Issue Tracker

This log tracks bugs, user complaints, and regressions. Every entry must include a **Root Cause Analysis (RCA)** to prevent recurrence.

| Date/Time | Issue | Root Cause Analysis | Fix |
| --- | --- | --- | --- |
| 2026-03-24 00:30 | Experiment Detail Pages Redirection | Sanity-based fetch was failing due to missing/incomplete data, and the hardcoded fallback lacked the `props` needed for `ExperimentLayout`, causing a route failure/redirect. | Migrated to local-first Content Collections. |
| 2026-03-24 00:46 | ExperimentCard Syntax Error | Manual refactor accidentally stripped the opening `---` frontmatter fence, causing an Astro compiler failure. | Restored frontmatter fences. |
| 2026-03-24 01:00 | Invisible Experiment Cards | GSAP animations on the index page were targeting `.post-card` but were not triggered correctly, and cards had hardcoded `opacity-0`. | Removed hardcoded `opacity-0` and refined GSAP triggers. |
| 2026-03-24 01:15 | Continuous Issue & Decision Logging | To prevent development cycles and regressions per user request. | Agent must read logs before every planning/execution phase. |
| 2026-03-24 01:30 | Live System Heartbeat | Every page should have a live "System Sync" clock to reinforce the "Live Lab" identity. | Implemented real-time clock in Footer.astro. |
| 2026-03-24 01:30 | Footer Clock Stale | The "Last System Sync" script only ran once on load and lacked a `setInterval` or Astro lifecycle handling. | Implementing `setInterval` and `astro:after-swap` listener. |
| 2026-03-24 01:45 | LabGallery Visibility | The LabGallery component was not rendering due to a missing conditional check for content availability, causing it to be hidden when no items were present. | Added a conditional render check to ensure LabGallery only displays when content is available. |
| 2026-03-24 02:15 | Fleet API Maintenance Overhead | Custom TS listener for Tesla Fleet API was becoming too complex to maintain (OAuth/signing). | Migrated to self-hosted TeslaMate instance on Raspberry Pi. |
| 2026-03-24 04:45 | Full Codebase Audit — 25 Fixes | Deep-dive audit revealed: HTML tag mismatch (ExperimentLayout), hardcoded secrets (GA/GTM/Turnstile/Sanity), duplicated post-fetching logic across 4 pages, missing prefers-reduced-motion on 4 components, event listener leak in HeroCanvasNeural, PostCard targeting non-existent CSS class, duplicate CSS variables, no font loading, missing .env.example, missing aria-labels, unused dead code files, reading time miscalculation, markdown syntax in .astro, and invalid robots.txt Sitemap entry. Issues #22 (PortableText) and #24 (_template.md) were false positives. | Created shared `posts.ts` utility, `.env.example`. Fixed all files. Deleted unused `HeroCanvas.astro` and `hero.glsl`. |
| 2026-03-24 13:45 | Content Integrity Audit — 20 Fixes | Cross-article review found: contradictory "Local by default" tagline vs cloud-first code, fake "Minimax M2.5" model reference, OpenAI in narrative but Together AI in code, unsubstantiated "100% grid-independent" and "15% vampire power" claims, inconsistent Pi count (1/3/4), "kernel panic" from OOM (technically inaccurate), Chrome "stealing VRAM" (incorrect), exposed Ollama API with no auth, privacy claim contradicting cloud usage, `...` and `your_module` code placeholders, ZeroMQ in diagram never explained, forced Tesla analogies, "fork" vs standard install, and duplicate publish dates. | Corrected all 20 issues across 8 blog posts and 3 experiment articles. Standardized Pi specs to 3× Pi 5 16GB + M.2 256GB. |
| 2026-03-25 23:45 | Missing Beehiiv Environment Variable | `PUBLIC_BEEHIIV_PUBLICATION_ID` was missing from `.env.local`, causing the fallback UI message to be visible to the user. | Populated `.env.local` with the correct Publication ID and updated `.env.example` with instructions. |
| 2026-03-30 06:40 | Google Tag Manager Not Working | `PUBLIC_GTM_ID` environment variable was missing from `.env.local`, causing the Astro conditional block in `BaseLayout.astro` to bypass rendering the GTM script tags. | Added `PUBLIC_GTM_ID=GTM-MBGML7FV` to `.env.local` to satisfy the existing `import.meta.env` check without hardcoding secrets in source. |
| 2026-03-31 21:40 | No proof layer in experiment detail pages | The markdown is technically rich but lacks visual evidence (Grafana snippets, terminal logs). | **Backlog (Pending)**: Every new experiment must include at least one screenshot or `render_diffs` block as "proof". |
| 2026-03-31 23:28 | Illegible Global Header in Light Mode | The `<Header>` had a hardcoded `text-white` class and a semi-transparent dark background. When the white `<article>` paper scrolled underneath it, the text became illegible and the background washed out. | Replaced `text-white` with CSS-variable-backed `text-text-primary`. Shifted `.theme-light` binding from the `<article>` tag to `document.body` so the sticky header fully adopted light mode colors. |
| 2026-03-31 23:38 | Lost Scroll Progress Bar | The `ReadingProgress.astro` component's GSAP ScrollTrigger failed after Astro navigation because the inline script only executed once on initial page load. | Refactored GSAP logic into an `initProgress` function bound to `astro:after-swap`. |
| 2026-04-01 00:05 | 34-Second Mobile TBT (Lighthouse) | Massive `Three.js` nested loops and `GSAP` bound-box queries were executing synchronously on load, locking the mobile CPU before FCP. | Wrapped `initThree()` and `initHomeAnimations()` inside `requestIdleCallback()`. Capped WebGL pixel ratio to 1.5. Deferred Google Tag Manager via `setTimeout(..., 3500)`. |
| 2026-04-11 | Orphaned event listeners after View Transitions (5 components) | Repeat of the 2026-03-24 / 2026-03-31 inline-script pattern: five components (`BlogLayout` copy-link, `ContactForm` async submit, `GifPlayer` play/pause, `CodeBlock` copy-code, `LabGallery` Motion `inView`) queried the DOM and attached listeners once at module top-level. On View Transitions the old DOM is swapped out, listeners go with it, and the new DOM's elements have no handlers. User-facing symptoms: Copy Link silently no-ops on post→post nav, Copy Code silently no-ops on any post reached via transition, GIF play/pause dead on newly-navigated posts, `/contact` form loses async success overlay (falls back to native Formspree POST), `/about` lab photos skip their Motion animation when reached via nav. | Wrapped each component's logic in a named `init<X>()` function, called once, then `document.addEventListener('astro:after-swap', init<X>)`. Matches the existing pattern used in `index.astro`, `ReadingProgress.astro`, `BaseLayout.astro`, `Footer.astro`, and `HeroCanvasNeural.astro`. |
| 2026-04-15 | Scroll listener accumulation in Header.astro | `initMenu()` is bound to `astro:after-swap`. Each call added a new `window.addEventListener('scroll', ...)` without removing the previous one. After N navigations, N scroll handlers fired simultaneously, causing jank and incorrect scroll state. | Added module-scoped `scrollController: AbortController` — each `initMenu()` call aborts the previous controller before creating a new one, passing `{ signal }` to the scroll listener. |
| 2026-04-15 | text-white in Footer.astro bypasses light-mode CSS variables | Three instances of `text-white` in `Footer.astro` (brand name "gekro", two `<span>` emphasis elements) were hardcoded. Under `.theme-light` they appeared white on a white background. The 2026-03-31 Header fix addressed the sticky header but left the footer untouched. | Replaced all three `text-white` instances with `text-text-primary` which resolves correctly through CSS variables in both dark and light mode. |
| 2026-04-15 | Mock newsletter form in index.astro sent no data | The homepage newsletter form simulated an 800ms delay then toggled a success CSS class but never POST-ed to Beehiiv or any backend. Users who subscribed via the homepage were silently dropped. | Replaced the mock form and its JavaScript with `<NewsletterEmbed />` — the same real component used in BlogLayout. |
| 2026-04-15 | Mermaid listener stacking in ExperimentLayout.astro | `<script is:inline>` re-executes on every page load (not just once per session). Each experiment page visit added another `document.addEventListener('astro:after-swap', initMermaid)` listener. On the Nth experiment page, initMermaid ran N times per swap, causing duplicate SVG renders. | Changed `<script is:inline>` to a regular module `<script>` (executes once per session). Accessed the CDN-loaded `mermaid` global as `(window as any).mermaid` to satisfy TypeScript strict mode. |
| 2026-04-15 | GiscusComments theme not synced with light reader mode | `loadGiscus()` always used `dark_dimmed` theme regardless of the user's current reader mode. When the ThemeToggle was active, Giscus rendered dark in a light page. | Added `document.body.classList.contains('theme-light')` check before injecting the Giscus script. Passes `'light'` theme when active, `'dark_dimmed'` otherwise. |
| 2026-04-15 | alert() in ContactForm.astro on submission errors | Network and Formspree API errors triggered `window.alert()` — a jarring, unstyled browser modal that breaks the UX. | Replaced both `alert()` calls with a `showError(msg)` helper that populates an inline `<p id="form-error">` element (hidden by default, auto-hides after 6s). |
| 2026-04-15 | RSS feed description used only `description` field | `rss.xml.ts` only sent `post.data.description` as the item description — the shortest and least informative field. AI readers and podcast apps received minimal context. | Updated to `post.data.aiSummary \|\| post.data.tldr \|\| post.data.description`, matching the SEO description priority chain used in BlogLayout and SEOHead. |
| 2026-04-15 | Dead inline style on PostCard.astro glow layer | The radial gradient on the glow div was set via `style="background: radial-gradient(300px ...)"` which was always overridden by a `!important` rule in the component `<style>` block. The mouse-position glow never actually showed the inline value. | Removed the inline `style` attribute entirely, added `.glow-layer` class to the div, and updated the CSS selector to target `.glow-layer` (no `!important` needed). |
| 2026-04-15 | Beehiiv iframe renders white on dark site | Beehiiv's iframe embed is cross-origin — their internal CSS forces `background: white` and it cannot be overridden from outside via CSS injection (same-origin policy). The hardcoded `height="339"` also clipped the subscribe button. User-visible symptom: jarring white box in an otherwise all-dark UI, button cut off at bottom. | Replaced the `<iframe>` in `NewsletterEmbed.astro` with a native dark-styled `<form>` that POSTs to the same Beehiiv subscribe endpoint via `fetch()` with `mode: 'no-cors'`. See decision log 2026-04-15 for no-cors trade-offs. |
| 2026-04-15 | Vite dep scanner misread JSDoc literal `<script>` substrings | Astro frontmatter JSDoc blocks in `GiscusComments.astro`, `ContactForm.astro`, and `JsonLD.astro` contained literal `<script>` tag strings in comment prose (e.g. "injects a fresh `<script>` tag on every swap"). Vite's dependency scanner treated them as real script tags and threw "Expected ';' but found 'on'" parse errors in dev mode. | Rephrased all three doc blocks to say "script tag" or "script element" without angle brackets so the scanner never sees `<script` inside frontmatter. |
| 2026-04-16 | Google Search Console: "Alternate page with proper canonical tag" on all non-root pages | Cloudflare Pages serves both `/blog` and `/blog/` as identical pages (no built-in preference). `Astro.url.href` at SSG build time produces trailing-slash pathnames (e.g. `https://gekro.com/blog/`). Google crawls `/blog` (no trailing slash), sees canonical pointing to `/blog/`, and marks `/blog` as "Alternate" rather than canonical — excluding it from indexing. | Three-part fix: (1) `SEOHead.astro` — derive canonical from `SITE_ORIGIN + Astro.url.pathname.replace(/\/$/, '')` instead of `Astro.url.href`; (2) `astro.config.mjs` sitemap `serialize()` hook strips trailing slashes from all non-root sitemap URLs; (3) `public/_redirects` 301-redirects `/*/ → /:splat` at the Cloudflare Pages edge so trailing-slash requests never reach the no-slash page as a duplicate. |
| 2026-04-17 | Build broken: `@astrojs/sitemap` 3.7.1 throws "Cannot read properties of undefined (reading 'reduce')" | Sitemap integration 3.4+ reads route data from the `astro:routes:resolved` hook, which was only added in Astro 5. With our `"^3.0.0"` spec, pnpm resolved to 3.7.1, so `_routes` stayed undefined during the Astro-4 build and `_routes.reduce` crashed in `astro:build:done`. The canonical URL fix earlier the same day did not cause this — the plugin's reduce runs regardless of `serialize` — but the canonical work was what triggered a local rebuild that surfaced the latent breakage. | Pinned `@astrojs/sitemap` to `3.2.1` (last Astro-4-compatible release) in `apps/web/package.json`, ran `pnpm install`. Until migration to Astro 5, any sitemap bump needs a paired Astro bump. |
| 2026-04-17 | Code-review sweep found self-XSS in LabTerminal command echo | `LabTerminal.astro:225` echoed the user's typed command back via innerHTML: `<span>rohit@gekro:~$</span> ${trimmed}`. Typing `<img src=x onerror=alert(1)>` executed. Self-XSS only (attacker and victim are the same user — same threat class as opening DevTools), but trivial to close. | Added an `escapeHtml()` helper on the Web Component and escape `trimmed` before interpolating into the innerHTML template. Other printLine callers either use textContent (default) or inject trusted hardcoded strings. |
| 2026-04-17 | **REGRESSION**: Every blog/experiment/topic link broken in production after canonical URL deploy | The `_redirects` rule shipped with the canonical URL fix used `/*/  /:splat  301` — a wildcard in the *middle* of the source path. Cloudflare Pages' `_redirects` format only officially supports `*` as a splat at the **end** of a source path. The middle-wildcard pattern was parsed permissively and matched `/blog/post-slug` → redirected to `/post-slug`, which 404'd, or in some cases produced redirect loops. User-facing symptom: clicking any post card on the homepage or `/blog` index returned a broken page. | Rewrote `public/_redirects` with explicit single-segment placeholder rules per route: `/blog/ → /blog`, `/blog/:slug/ → /blog/:slug`, same pattern for experiments/topics/about/contact/now/slides. `:placeholder` matches exactly one path segment per Cloudflare's documented syntax. Added an inline comment explaining the `*`-in-middle trap so the next editor doesn't repeat it. |
| 2026-04-17 | **REGRESSION 2 — ERR_TOO_MANY_REDIRECTS**: Per-route trailing-slash-stripping rules created infinite loop | Astro builds in directory format (`dist/blog/slug/index.html`). Cloudflare Pages auto-canonicalises requests for such URLs to the trailing-slash form (308 `/blog/slug` → `/blog/slug/`). The `_redirects` rule `/blog/:slug/ → /blog/:slug 301` then stripped the slash again, CF re-added it, loop. The whole trailing-slash-strip strategy from 2026-04-16 was incompatible with directory-format output. Original GSC signal ("Alternate page with proper canonical tag") was actually Google correctly respecting the canonical tag — never an indexing failure, so the 2026-04-16 fix was unnecessary. | Reverted the full canonical strategy: (1) `SEOHead.astro` back to `Astro.url.href` (natural trailing-slash); (2) removed sitemap `serialize()` hook; (3) emptied `public/_redirects` (comment-only) to let CF Pages handle canonicalisation natively. All three URL surfaces now agree on the trailing-slash form. |
| 2026-06-02 | Reflected XSS in markdown-visualizer via `?md=` shareable URL | `esc()` escaped `&<>` but not `"`, so a crafted image/link target (`![x](" onerror="…)`) broke out of the double-quoted attribute; the `javascript:` filter was also bypassable (`java\tscript:`). The app auto-renders the base64 `?md=` param on load, so a crafted link = one-click cross-user script execution on the gekro.com origin. | `esc()` now escapes quotes; new `safeUrl()` strips control chars then allowlists http/https/mailto/tel (else `#`). |
| 2026-06-02 | Self-XSS in llm-response-unpacker / json-schema-to-tool / dummy-data-generator | User-pasted JSON tool-call names+args, schema property keys, and column field-names were interpolated raw into `innerHTML`. Self-inflicted (user supplies the content) but a real DOM-injection sink. | Added HTML-escaping on every interpolated user value. |
| 2026-06-02 | Live-`innerHTML` HTML parsing in rich-text-to-markdown | `htmlToMd` assigned untrusted HTML to a live element's `innerHTML` and the paste path inserted cloned nodes into the contenteditable, so `<img onerror>` would fire. | Parse inertly via `DOMParser`; sanitize pasted HTML (strip script/style/embed, `on*` attrs, `javascript:` URLs) before insertion. voice-transcriber transcript now rendered via `textContent`. |
| 2026-06-02 | AppShell `app:*` listeners leaked across ~35 apps | `{ signal: ac.signal }` was missing or misplaced into an inner `Blob`/`Object.assign`/`forEach`/`map`, so the document-level `app:copy/reset/export` listeners were never aborted on `astro:before-swap` and stacked across View-Transition navigations (duplicate exports/clipboard writes). device-info had no AbortController at all; csv-to-json had a 4-arg `addEventListener`. **Process note:** the review subagents first waved several "signal as bogus Blob 3rd arg" cases through as harmless — a ground-truth grep + a per-listener verification script caught 5 real ones (dice-roller, docker-compose-visualizer, finetuning-formatter, gradient-generator, password-generator) they'd missed. | All `app:*` listeners scoped to the AbortController; verified 0 unscoped + 0 misplaced across all 69 apps. |
| 2026-06-02 | css-button-generator dropped `none`-valued declarations | `['border','box-shadow','transition'].includes('')` is always `false`, so the filter collapsed to `v !== 'none'` and silently removed `border`/`box-shadow`/`transition: none` from the generated CSS. | Changed `.includes('')` → `.includes(k)`. |
| 2026-06-02 | tesla-trip-calculator efficiency-override field ignored | `getEff()` read `parseFloat(model.value…) || effField`; the model value is always numeric so the `||` never reached the user's override input — typing in it did nothing. | Made the (model-seeded, editable) efficiency field the source of truth. |
| 2026-06-21 | 8-app targeted bug sweep (all fixes in private `gekro-apps`) | (1) **unix-timestamp-converter**: `datetime-local` default built from `toISOString()` (UTC) but the input reads as LOCAL → off by tz offset on initial render + reset; ms-detection `v > 9_999_999_999` failed for negative pre-1970 ms. (2) **password-generator**: generated passwords (symbol pool incl. `< > &`) interpolated raw into `innerHTML` → display/copy corruption + self-injection. (3) **dice-roller**: player name interpolated raw into innerHTML and `value="${name}"` → self-XSS / attribute breakout. (4) **device-info**: `row(key,val)` built innerHTML from navigator.*, WebGL UNMASKED_RENDERER, and the remote 1.1.1.1 trace response without escaping. (5) **streaming-response-player**: TTFT structurally always `<1ms` — `chunks[0].t` is normalized to 0 then reported as TTFT. (6) **translator**: first-translation no-op race — changing language mid model-download discarded the in-flight run via the runId guard while the concurrent run returned early on null `loadModel()`, leaving output empty + status stuck on "Loading model…". (7) **mcp-trace-visualizer**: JSON highlight regexes ran AFTER HTML-escaping so they matched `"` (now `&quot;`) and never fired. (8) **voice-transcriber**: a surfaced error ("Microphone access denied") was wiped — onerror set the intentional-stop flag, onend → finalizeSegment reset live text to the placeholder. | (1) Added `localDateTimeValue()` from local getFullYear/getMonth+1/getDate/getHours/getMinutes with zero-pad, applied at init + `app:reset`; `isMs` now `Math.abs(v) > 9_999_999_999`. (2) Render each password via `document.createElement` + `textContent` (crypto untouched). (3) Added `escHtml`, escape name in the value attr + per-player results + history detail. (4) Added `esc` helper, escape key+val in `row()`. (5) `parseLog` now returns `{chunks, ttft}` capturing the first chunk's ORIGINAL timestamp before normalize-to-0; `recompute` reports `parsed.ttft`. (6) On successful `loadModel()` resolve, if input box has non-empty text, fire `run()` once (re-uses cached `translate`, no re-download). (7) String/key regexes rewritten to match the escaped form (`&quot;…&quot;`); escaping kept (no highlight-before-escape). (8) Added `errorActive` flag set in onerror, guards finalizeSegment's empty-reset + setRecordingUI placeholder-restore; cleared on new recording start + `app:reset`. All scaffold preserved (idempotency guards, AbortController-scoped `app:*` listeners, `astro:after-swap`); design tokens only; not built per instruction. |
| 2026-06-22 | /news read as stale — the same multi-day story (Fable 5 export ban) was re-reported every morning June 13–21 | Each daily briefing is generated in isolation: `generate-briefing.mjs` never saw what it had already published, so the model re-picked the biggest ongoing story while fresh follow-up articles kept passing the 36h freshness filter. The filter checks the FEED ITEM date and cannot distinguish "new article about an old story" from "new story." Owner noticed a June-12 ban still leading the feed on June-22. | Added `loadRecentBriefings(6)`: the generator reads the last 6 published briefing titles and instructs the model NOT to re-report them unless there is a genuinely new, material development. Prompt-side dedup, no schema change. |
| 2026-06-22 | Auto-published briefings cited fabricated / low-trust URLs (`FreeFable.org`, `mangomindbd.com`, `coursiv.io`) mixed with real outlets | The prompt sent the model only `[source · age] title + desc` and NOT the feed item URLs, so the model generated every citation link from memory; the validation gate only checked `new URL()` (syntactic validity), never that a URL was real or supplied. On a fast-moving story the model confabulated plausible dated paths and even invented whole domains — a citation a "learned listener" could click and find dead. | (1) real feed `item.link` now included with every candidate headline; (2) prompt says cite ONLY those URLs verbatim and report only what the headlines support; (3) the validation gate rejects, fail-closed, any `sourceUrl` not in the supplied allow-list (normalised origin+path). A fabricated citation can no longer publish. Existing posts keep old citations (Content Protection — not edited). |
| 2026-06-23 | 11 apps scrolled the whole page horizontally (mobile + desktop) | Found by the new Playwright apps smoke suite, which loads every `/apps/[slug]` at 375/768/1280px and fails on document-level horizontal scroll. Two root causes. (A) METHODOLOGY prose: wide markdown tables (base64-encoder, device-info, finetuning-formatter, json-schema-to-tool, llm-response-unpacker, systemd-unit-generator) and long unbreakable tokens (nginx-config-generator) had no mobile constraint, so they pushed the page sideways - the existing `.prose pre` rule only covered fenced code blocks, not tables or inline tokens. (B) Per-app layout: code-snippet-png's fixed-width capture preview; tesla-charge-optimizer's 24-hour TOU grid (overflowed the desktop max-w-5xl left column); css-button-generator's `flex-1` range sliders (min-width:auto, could not shrink); hyperscaler-comparison's non-wrapping sort row. | (A) global.css: prose tables now `display:block; max-width:100%; overflow-x:auto` (scroll within column) + `overflow-wrap:break-word` on prose text and `anywhere` on inline code (site-wide; also helps blog/experiments/stack). (B) gekro-apps@2643c6b: code-snippet-png wrapper `overflow-x-auto max-w-full`; tesla `.tco-hour-grid overflow-x:auto`; css-button range `min-w-0`; hyperscaler sort row `flex-wrap`. Verified: 72/72 apps pass the smoke (0 overflow at all 3 widths). |
| 2026-07-05 | 41-bug correctness sweep: exhaustive per-app logic review (36 apps deep-read) + new interaction/fuzz e2e layer | Owner escalated ("you keep finding bugs along the way") demanding one exhaustive pass instead of incremental discovery. Two instruments: (A) 3 parallel review agents read 36 apps line-by-line against their stated methodology, strict confirm-with-concrete-trigger bar (agents for the remaining 36 apps were killed by the monthly spend limit - THAT REVIEW IS STILL OWED); (B) new `e2e/apps.interactions.spec.ts` drives every app on every load: hostile-string + boundary-numeric fuzz into every input, cycles selects, clicks all island buttons, fires app:copy/export/reset, then asserts no errors, no XSS-probe execution, island alive, no navigation. Findings: 37 review bugs + 4 fuzz bugs across 26 apps. Worst: bmi Reset corrupting weight by 2.20462x each press; cron parsing "1st of every month" as Monday; graphing-calculator breaking on ALL pi expressions (Math.Math.PI) and on -x^2 (JS unary-minus-before-** SyntaxError, blank Peaks preset); Mac unified-memory double-count recommending impossible offload; Opus 4.5+ priced 3x high; India UTC offset off by an hour; agent-loop freeze on pasted 999999999 steps (uncapped compute loop). | All 41 fixed in gekro-apps@158d309 (26 Calculator.astro files). Test harness hardened: shared e2e/helpers.ts allowlist, actionTimeout 5s, per-test fuzz time budget 55s, code-snippet-png/rag-eval headless-noise allowances. Verified end-state: vitest 80/80, Playwright 144/144 (72 smoke + 72 interaction), build clean. NOTE the honest gap: 36 apps (lora-memory -> word-counter alphabetically) still need the same line-by-line logic review when API limits reset - they are covered by fuzz/smoke/prior 2026-06-21 sweep but not by this depth of math verification. |
| 2026-07-05 | Review round 2 - final 36 apps deep-reviewed in-session, 20 more bugs fixed (ALL 72 APPS NOW COVERED) | Spend limit killed the subagent fleet, so the remaining 36 apps (lora-memory -> word-counter) were reviewed line-by-line in the main session, math traced against each app's stated methodology. Worst finds: (1) tax-loss-harvester collected YTD ST/LT gains but IGNORED them - taxSaved was flat |loss|x-rate and carryforward was "harvested - $3k", so $50k gains + $20k harvest claimed a $17k carryforward (correct: $0); (2) options-pnl told covered-call/CSP users their max loss was "limited to premium paid" (it is stock-to-zero) and computed spread Return-on-Premium against the long leg's premium instead of net debit; (3) rag-chunk-inspector's splitRecursive tested "is last part" BY VALUE, silently deleting the separator after any middle piece equal to the last one - corrupted chunk text; (4) prompt-diff's O(mxn) LCS had no size cap (multi-hundred-MB alloc freeze) and ran the full LCS TWICE per keystroke; (5) prompt-cache-optimizer clamped savings at $0, hiding that caching loses money at low hit rates under the 1.25x write surcharge; (6) reasoning-cost's "monthly" was annual/12 in the headline but daily*30 in the table - same model, two numbers. Plus NaN-edge renders in position-sizer/lora-memory/tesla-trip/options-pnl, Haiku-4 vision priced at the 3.5 rate, and tesla-charge printing "22:60". | All 20 fixed in gekro-apps@57d54f7 (11 files). Clean after full review: prompt-token-counter, tokenizer, token-probability-visualizer, sampling-playground, regex-playground, text-diff, word-counter, markdown-visualizer, system-prompt-linter, ssh-config-generator. Spot-verified clean: model-benchmark, mcp-server-tester, pdf-merger, websocket-tester, rag-eval-toolkit. Re-confirmed from the 2026-06-21 verified round: nginx, systemd, unix-timestamp, streaming, translator, mcp-trace, voice-transcriber, password-generator, rich-text-to-markdown. End state: 144/144 Playwright, vitest 80/80, build clean. Combined with the 07-05 morning round: 61 bugs found+fixed across the two-day exhaustive audit; every one of the 72 apps has now had logic-level review. |
| 2026-07-06 | Production froze on a stale build (visible symptom: the site still showed "72 apps" after Wave 4 shipped 78) and GitHub Actions CI went red — every Cloudflare Pages deploy had been silently failing. | Both build systems clone the private `gekro-apps` repo with a fine-grained GitHub PAT (`GITHUB_PAT` in CF Pages, `SUBMODULE_PAT` in Actions). Both tokens were created together and **expired on the same day**. CF Pages died at the very first build step — `git clone https://x-access-token:***@github.com/drajb/gekro-apps.git ...` → `remote: Invalid username or token. Password authentication is not supported ... Failed: build command exited with code: 128` — so Astro never ran and CF kept serving the last successful build (pre-Wave-4, hence 72). CI failed identically at "Clone private apps components" (~11s fail). The count logic itself was never wrong: the built HTML renders "78 free tools" from `getCollection('apps').length`; the stale number was purely a not-deployed artifact. | Rohit generated a new fine-grained PAT (Contents: Read-only on `drajb/gekro-apps`) and updated **both** secrets — `GITHUB_PAT` (CF Pages → Variables & Secrets) and `SUBMODULE_PAT` (gekro → Actions Secrets). CF redeploy succeeded (all 78 apps + clock redesign + "78 free tools" now live); the CI re-run went green end-to-end (clone → typecheck → lint → tests → build, 1m29s). **Prevention:** set the replacement PAT to max expiry and rotate both together; when the site freezes on old content, check the CF build log for this clone-auth error first. Also fixed the one genuinely hardcoded count (`public/llms.txt` 72→78). Documented in CLAUDE.md §8 + decision-log 2026-07-06 + dual-repo memory. |
| 2026-07-25 | Site search silently broken in production (empty box on /blog, no console signal) | Pagefind 1.5 changed `pagefind-ui.js` from an ES module with a named `PagefindUI` export to a bundle that ASSIGNS `window.PagefindUI`. `const { PagefindUI } = await import(...)` therefore yielded `undefined` and `new PagefindUI(...)` threw "not a constructor". Two things hid it: the catch logged a bare `console.log` with the error object discarded, and the `#search-unavailable` fallback was wrapped in `{import.meta.env.DEV && ...}` so it did not exist in production. Net effect: `#pagefind-search` had `data-pagefind-wired="true"` and `innerHTML.length === 0`, i.e. a blank box with nothing to diagnose. Found by probing the live DOM during an SEO audit, not by any test or alert. | Resolve the constructor from `mod.PagefindUI ?? window.PagefindUI` and throw explicitly if neither is a function (works across the version change in either direction); `console.warn` WITH the error; render the fallback message in production too so a future failure degrades to an explanation instead of an empty box. Verified against the real Pagefind bundle by serving `dist` locally: widget renders, `/blog/?q=raspberry` pre-fills and returns 5 results, fallback hidden, 0 console errors. |
| 2026-07-25 | News was an orphaned cluster — 53 briefings with 3 inbound internal links each (vs 10 for a blog post) | Every briefing carried a `topics` frontmatter array that was never rendered or linked anywhere, and `getAllPosts()` (which feeds /topics/[topic]) reads only `getCollection('blog')`, so news never entered the topic system at all. The topic strings were also free-form: 53 files produced 113 distinct labels that fragmented one concept across spellings ("open-weights" / "open-weight models" / "open-source-models"). Result: the whole /news archive was reachable only chronologically and accrued no topical authority. | Added `lib/utils/news-topics.ts` (NEWS_TOPIC_MAP → 35 canonical names, unmapped labels dropped rather than guessed, MIN_HUB_ARTICLES=3 to avoid thin hubs); topic hubs now union blog + eligible news topics keyed by SLUG (duplicate paths are a build error); news articles render topic chips linking only to topics that actually have a hub. Canonical names deliberately match existing blog topics so LLMs/AI Agents/Hardware/Infrastructure/APIs merge rather than splitting. 20 → 44 hubs, inbound links per briefing 3 → 7. |
| 2026-07-25 | Verification sweep — 6 app logic bugs, incl. a repair tool that corrupted valid input | **llm-json-repair (worst):** `parseString` preserved escape sequences in their ENCODED form (`out += c + nx`) while the parsed value is re-encoded by `JSON.stringify` on output, so every standard escape was double-escaped. Valid input `{"msg":"line1\nline2"}` came back as `line1\nline2` (a literal backslash and n, not a newline) and `A` as `\u0041` instead of `A`. A tool whose entire purpose is repairing JSON silently corrupted already-correct JSON, and the status line still read "already valid"/"repaired ✓". Proven against production before fixing. **amortization-calculator:** `Math.pow(1+r, n)` overflows to Infinity at extreme rate/term, and Infinity/Infinity is NaN — "$NaN" rendered in the schedule and all four refi figures. The existing guards only proved the INPUTS were finite, never the result. **options-pnl:** `(s / spot)` had no divisor guard even though the Return-on-Premium line directly below it did — zero/cleared spot printed "NaN%" in every row. **position-sizer:** `(equity / account)` likewise unguarded — "+NaN%" under every Monte Carlo sizing method. **gguf-inspector:** KV-cache estimate assumed KV dim == embedding_length (multi-head attention); nearly every modern model uses GQA where n_kv_heads is 4-8x smaller, so it overstated memory by that factor and could wrongly report a model won't fit. **rate-limit-planner:** every input flipped the provider preset to Custom, including workload fields, so adjusting "target req/min" wiped the tier family and silently suppressed the "minimum tier that clears this" suggestion. Root cause common to the three NaN bugs: guards validated inputs but never the computed result, and no automated layer ever asserted on rendered output. | All six fixed in gekro-apps@6e1c7b8 with the divisor/result guards added at the point of computation. Added `e2e/apps.output-sanity.spec.ts` (gekro@f1c93d7): drives all 78 apps to numeric edges (0, empty, -1, huge) and fails if NaN/Infinity/undefined/[object Object] reaches the DOM — it found 3 of the 6 on its first run. Suite 156 -> 234 tests, all green. Also verified mechanically across all 78 apps: idempotency guards, astro:after-swap re-registration, AbortController presence and listener scoping all clean (0 violations); the 5 "no HTML escaper" and 25 "no numeric guard" grep flags were all confirmed false positives (numeric-only interpolation / string tools). |
| 2026-07-26 | App model/pricing catalogs three generations stale; two were wrong enough to mislead | Hand-maintained catalogs carried `LAST_VERIFIED = 2026-04-19` and topped out at Opus 4.7 / Sonnet 4.6 / GPT-5.4 / Gemini 2.5, while the site's own /news feed had been reporting Sonnet 5, Opus 5, Fable 5 and the GPT-5.6 family for weeks. Beyond staleness, two entries were actively wrong: **agent-loop-cost-estimator** priced Claude Opus 4.8 at $15/$75 with a 200k window (actual: $5/$25 at 1M), so every agent-loop estimate on Opus came out ~3x too expensive, and both GPT-5.4 entries were roughly half price; **prompt-cache-optimizer** asserted a blanket 1024-token cache minimum for Sonnet/Opus and 2048 for Haiku, when minimums are per-model and the claim was wrong in both directions (Opus 5 = 512, Haiku 4.5 = 4096). Separately, the weekly hyperscaler pricing pipeline was green but its data had been frozen on `main` since 2026-06-01: the job opens/updates PR #22 (`pricing/auto-update`) and nobody had merged it for 8 weeks — automation that "succeeds" while shipping nothing, the same silent-success shape as the 2026-07-25 news pipefail bug. | Re-verified every number against the vendors' own pricing pages (platform.claude.com, developers.openai.com, ai.google.dev) and refreshed 5 apps in gekro-apps@f415e74; `LAST_VERIFIED` now 2026-07-26. Merged PR #22 (contents were `last_verified` date bumps only — no hyperscaler price had actually moved since June, so the data was accurate, only the stamp was stale). Fixed the default selections and dropdown options that pointed at removed IDs. Verified: astro check 0 errors, build clean, 234/234 e2e, new models render in the built dropdowns. **Still owed:** three methodology markdown files (`llm-cost-calculator.md`, `llm-response-unpacker.md`, `model-benchmark.md`) quote the old prices/models in prose and are under the Content Protection Protocol, so they were NOT edited — they need a per-file instruction. `model-benchmark`'s MMLU/GPQA/HumanEval scores are also pre-Opus-5 and cannot be refreshed without published numbers (repo rule: never estimate a benchmark from specs). |
| 2026-08-28 | `inr()` currency formatter returned a broken string for every value | Operator precedence. The body was written as `'₹' + Math.round(...) >= 0 ? ... : ''`, so `+` bound tighter than `>=` and the condition compared a *string* to `0`, which is always false. Every call would have returned `''`. Caught before any UI was built on it. | Rewrote as a plain function with an explicit `Number.isFinite` guard. Lesson: a ternary whose condition contains `+` needs parentheses or a statement body. |
| 2026-08-28 | India tax engine: my first monotonicity test asserted a property the law does not have | I asserted that take-home never falls as income rises. It does fall, twice: marginal relief caps *tax* at the excess income earned, and then 4% cess is charged on top of the capped figure, so inside any relief band an extra rupee costs ₹1.04. The old regime's ₹5,00,000 87A cliff has no relief at all and steps by about ₹13,000. The engine was right; the test was wrong. | Replaced with the precise property: total tax is monotone non-decreasing everywhere, and take-home may only dip while `cliffRelief > 0` or `surchargeRelief > 0`, bounded by the cess rate. Both quirks are now documented in the app methodology instead of being hidden. |
| 2026-08-28 | `apps-private/` local mirror is a stale clone, not a synced one | Its HEAD sits at the original 51-component commit while its working tree has been kept current by file copies from earlier sessions, so `git status` there shows ~90 spurious modifications. It is gitignored in `gekro` and the build clones `gekro-apps` HEAD fresh, so this is cosmetic - but it looks alarming and invites a wrong fix. | Left as-is deliberately; new apps are copied in for local dev only. Never commit inside `apps-private/`. Authoritative source is always `G:/Git/gekro-apps`. |
| 2026-08-28 | I stamped the three regional apps with the wrong date, 2026-08-21 instead of 2026-08-28 | I carried a date forward from earlier in the session instead of checking the system clock, and never validated it. It propagated into `publishedAt`, `lastVerified`, every "verified 2026-08-21" source line, the in-app disclaimer text, both governance logs and three memory files. Nothing computed was affected, but `lastVerified` is a user-visible freshness claim on tax and legal content, so a wrong one is a false claim. | Corrected everywhere with a scripted sweep across both repos and the memory directory, verified zero remaining occurrences. Lesson: run `date` before writing any date into published content rather than trusting a value carried through a long session. |
| 2026-08-28 | Apple Silicon data was a full generation stale in four shipped apps | `gpu-vram-calculator` (verified 2026-04-20), `local-model-recommender`, `inference-latency-estimator` and `llama-cpp-config-builder` all listed M2/M3/M4 machines as the current lineup. Apple launched the M6 / M5 Pro mini and M5 Max / M5 Ultra Studio on 25 Aug 2026, taking the flagship from 192 GB / 800 GB/s to 512 GB / 1.2 TB/s. Nothing prompts a re-check of hardware tables when the vendor ships, so they simply rotted. | Refreshed all four plus their methodology docs, and set `lastVerified: 2026-08-28` on each (adding the field to the two that lacked it). The underlying gap remains: there is no scheduled review for hardware tables the way there is for the pricing pipeline. |
| 2026-08-28 | Same chip carries different TFLOP figures in different apps | M4 Max is 18 TFLOP/s in `local-model-recommender` and 34 TFLOP/s in `inference-latency-estimator`. Both predate this session. The two apps use the number for different purposes and on different scales, and I could not establish which is right without a source neither app cites. | NOT fixed - deliberately. I scaled the new M5 entries within each app's own existing convention so nothing got worse, and logged the inconsistency rather than guessing. Resolving it needs a cited FLOP source, which Apple does not publish. |
| 2026-09-02 | My first chart audit under-counted the apps that already had visuals | I grepped only for `<svg>` and `getContext('2d')`, which missed five apps whose charts are built from divs with a data-driven `style="width:${pct}%"`. I was one edit away from adding a redundant SVG stacked bar to gpu-vram-calculator, which already has a perfectly good div-based one with a legend. | Re-ran the audit with a pattern that catches data-driven div dimensions, and skipped all five. Lesson: "does this app have a chart" is not the same question as "does this app contain an svg tag". |
| 2026-09-02 | Private-repo pushes alone do not deploy to production | Calculator components live in drajb/gekro-apps, but Cloudflare Pages builds on pushes to the PUBLIC drajb/gekro repo and clones gekro-apps HEAD during that build. Two chart tranches went to the private repo only, so the work was on main in both places and still not live. | Pushed a public-repo commit (these log entries) to trigger the rebuild. Worth remembering for any private-repo-only change: it ships when something next pushes to the public repo, not when you push it. |
| 2026-09-02 | Heredoc-authored patch scripts silently ate backslashes and mismatched line endings | Two separate failures while wiring charts. First, `<<'PY'` heredocs dropped a backslash so `join('\n')` became a real newline and anchors missed. Second, patch anchors written with LF did not match older Calculator files that git had checked out as CRLF, so `assert count == 1` fired on text that was visibly present. | Stopped authoring patch scripts through heredocs - they go through the Write tool now - and `patchlib.rep()` normalises every anchor to whatever line ending the target file actually uses. Both failures were caught by asserts before any file was written, which is the only reason they were annoyances rather than corruption. |
| 2026-09-02 | tesla-trip-calculator asked for ¢/kWh and $/kWh in adjacent fields | Reported by Rohit. Home electricity was labelled ¢/kWh with a default of 17.45 sitting directly above Supercharger rate labelled $/kWh with a default of 0.28. The maths was correct - the code divided the home value by 100 in all three places it read it - so nothing computed wrong. The hazard was purely that a reader sees $/kWh on one field and types 0.17 into the other, getting a 100x under-estimate with no error. | Standardised both on $/kWh (home default 0.1745, step 0.5→0.01, /100 removed from all three read sites, result card and CSV export updated). Verified in-browser that outputs are byte-identical to before: 77.6 kWh × $0.1745 = $13.55 and × $0.28 = $21.74, ratio matching 0.28/0.1745 exactly. |
| 2026-09-02 | ev-charging-cost mixed a 0-1 fraction and a 0-100 percentage in one panel | Found during the sweep prompted by the Tesla report. Solar offset was a 0-100 slider showing '%', while charger efficiency directly below it was a bare 0.5-1.0 fraction whose label carried no unit at all. Same trap: two ratio inputs, two conventions, adjacent. | Charger efficiency converted to a percentage (50-100, default 88) with '(%)' added to the label. Preset option values stay as fractions - they are data - and are multiplied by 100 on apply. Verified: effective rate $0.114 matches 50/0.88×0.10/50, and switching to the Supercharger preset now sets 95 rather than 0.95 and yields $0.421 as expected. |
| 2026-09-02 | GB means two different things across the memory apps - NOT fixed, flagged | apple-silicon-llm-configurator, gpu-vram-calculator, inference-latency-estimator, vector-db-calculator and gguf-inspector divide bytes by 1e9 (decimal GB). llama-cpp-config-builder, local-model-recommender and lora-memory-calculator divide by 1024³ (binary GiB). Both label the result 'GB'. That is a 7.4% discrepancy between apps that cross-link each other and are meant to agree, and it lands exactly where it matters - at the margin of 'does this fit in 64GB'. Binary is arguably the correct convention, since a '24GB' card and a '64GB' Mac are both really GiB. | Deliberately NOT changed in this pass. It is a maths and display change across eight apps rather than a label fix, it would move numbers verified earlier this week, and picking a convention is a judgement call worth Rohit's sign-off. Raised with a recommendation to standardise on binary GiB. |
