# Gekro Lab — Issue Tracker

This log tracks bugs, user complaints, and regressions. Every entry must include a **Root Cause Analysis (RCA)** to prevent recurrence.

| Date/Time | Issue | Root Cause Analysis | Fix |
| --- | --- | --- | --- |
| 2026-03-24 00:30 | Experiment Detail Pages Redirection | Sanity-based fetch was failing due to missing/incomplete data, and the hardcoded fallback lacked the `props` needed for `ExperimentLayout`, causing a route failure/redirect. | Migrated to local-first Content Collections. |
| 2026-03-24 00:46 | ExperimentCard Syntax Error | Manual refactor accidentally stripped the opening `---` frontmatter fence, causing an Astro compiler failure. | Restored frontmatter fences. |
| 2026-03-24 01:00 | Invisible Experiment Cards | GSAP animations on the index page were targeting `.post-card` but were not triggered correctly, and cards had hardcoded `opacity-0`. | Removed hardcoded `opacity-0` and refined GSAP triggers. |
| 2026-03-24 01:15 | Continuous Issue & Decision Logging | To prevent development cycles and regressions per user request. | Agent must read logs before every planning/execution phase. |
| 2026-03-24 01:30 | Live System Heartbeat | Every page should have a live "System Sync" clock to reinforce the "Live Lab" identity. | Implemented real-time clock in Footer.astro. |
| 2026-03-24 01:30 | Footer Clock Stale | The "Last System Sync" script only ran once on load and lacked a `setInterval` or Astro lifecycle handling. | Implementing `setInterval` and `astro:after-swap` listener. |
| 2026-03-24 01:45 | LabGallery Visibility | The LabGallery component was not rendering due to a missing conditional check for content availability, causing it to be hidden when no items were present. | Added a conditional render check to ensure LabGallery only displays when content is available. |
| 2026-03-24 02:15 | Fleet API Maintenance Overhead | Custom TS listener for Tesla Fleet API was becoming too complex to maintain (OAuth/signing). | Migrated to self-hosted TeslaMate instance on Raspberry Pi. |
| 2026-03-24 04:45 | Full Codebase Audit — 25 Fixes | Deep-dive audit revealed: HTML tag mismatch (ExperimentLayout), hardcoded secrets (GA/GTM/Turnstile/Sanity), duplicated post-fetching logic across 4 pages, missing prefers-reduced-motion on 4 components, event listener leak in HeroCanvasNeural, PostCard targeting non-existent CSS class, duplicate CSS variables, no font loading, missing .env.example, missing aria-labels, unused dead code files, reading time miscalculation, markdown syntax in .astro, and invalid robots.txt Sitemap entry. Issues #22 (PortableText) and #24 (_template.md) were false positives. | Created shared `posts.ts` utility, `.env.example`. Fixed all files. Deleted unused `HeroCanvas.astro` and `hero.glsl`. |
| 2026-03-24 13:45 | Content Integrity Audit — 20 Fixes | Cross-article review found: contradictory "Local by default" tagline vs cloud-first code, fake "Minimax M2.5" model reference, OpenAI in narrative but Together AI in code, unsubstantiated "100% grid-independent" and "15% vampire power" claims, inconsistent Pi count (1/3/4), "kernel panic" from OOM (technically inaccurate), Chrome "stealing VRAM" (incorrect), exposed Ollama API with no auth, privacy claim contradicting cloud usage, `...` and `your_module` code placeholders, ZeroMQ in diagram never explained, forced Tesla analogies, "fork" vs standard install, and duplicate publish dates. | Corrected all 20 issues across 8 blog posts and 3 experiment articles. Standardized Pi specs to 3× Pi 5 16GB + M.2 256GB. |
| 2026-03-25 23:45 | Missing Beehiiv Environment Variable | `PUBLIC_BEEHIIV_PUBLICATION_ID` was missing from `.env.local`, causing the fallback UI message to be visible to the user. | Populated `.env.local` with the correct Publication ID and updated `.env.example` with instructions. |
| 2026-03-30 06:40 | Google Tag Manager Not Working | `PUBLIC_GTM_ID` environment variable was missing from `.env.local`, causing the Astro conditional block in `BaseLayout.astro` to bypass rendering the GTM script tags. | Added `PUBLIC_GTM_ID=GTM-MBGML7FV` to `.env.local` to satisfy the existing `import.meta.env` check without hardcoding secrets in source. |
| 2026-03-31 21:40 | No proof layer in experiment detail pages | The markdown is technically rich but lacks visual evidence (Grafana snippets, terminal logs). | **Backlog (Pending)**: Every new experiment must include at least one screenshot or `render_diffs` block as "proof". |
| 2026-03-31 23:28 | Illegible Global Header in Light Mode | The `<Header>` had a hardcoded `text-white` class and a semi-transparent dark background. When the white `<article>` paper scrolled underneath it, the text became illegible and the background washed out. | Replaced `text-white` with CSS-variable-backed `text-text-primary`. Shifted `.theme-light` binding from the `<article>` tag to `document.body` so the sticky header fully adopted light mode colors. |
| 2026-03-31 23:38 | Lost Scroll Progress Bar | The `ReadingProgress.astro` component's GSAP ScrollTrigger failed after Astro navigation because the inline script only executed once on initial page load. | Refactored GSAP logic into an `initProgress` function bound to `astro:after-swap`. |
| 2026-04-01 00:05 | 34-Second Mobile TBT (Lighthouse) | Massive `Three.js` nested loops and `GSAP` bound-box queries were executing synchronously on load, locking the mobile CPU before FCP. | Wrapped `initThree()` and `initHomeAnimations()` inside `requestIdleCallback()`. Capped WebGL pixel ratio to 1.5. Deferred Google Tag Manager via `setTimeout(..., 3500)`. |
| 2026-04-11 | Orphaned event listeners after View Transitions (5 components) | Repeat of the 2026-03-24 / 2026-03-31 inline-script pattern: five components (`BlogLayout` copy-link, `ContactForm` async submit, `GifPlayer` play/pause, `CodeBlock` copy-code, `LabGallery` Motion `inView`) queried the DOM and attached listeners once at module top-level. On View Transitions the old DOM is swapped out, listeners go with it, and the new DOM's elements have no handlers. User-facing symptoms: Copy Link silently no-ops on post→post nav, Copy Code silently no-ops on any post reached via transition, GIF play/pause dead on newly-navigated posts, `/contact` form loses async success overlay (falls back to native Formspree POST), `/about` lab photos skip their Motion animation when reached via nav. | Wrapped each component's logic in a named `init<X>()` function, called once, then `document.addEventListener('astro:after-swap', init<X>)`. Matches the existing pattern used in `index.astro`, `ReadingProgress.astro`, `BaseLayout.astro`, `Footer.astro`, and `HeroCanvasNeural.astro`. |
| 2026-04-15 | Scroll listener accumulation in Header.astro | `initMenu()` is bound to `astro:after-swap`. Each call added a new `window.addEventListener('scroll', ...)` without removing the previous one. After N navigations, N scroll handlers fired simultaneously, causing jank and incorrect scroll state. | Added module-scoped `scrollController: AbortController` — each `initMenu()` call aborts the previous controller before creating a new one, passing `{ signal }` to the scroll listener. |
| 2026-04-15 | text-white in Footer.astro bypasses light-mode CSS variables | Three instances of `text-white` in `Footer.astro` (brand name "gekro", two `<span>` emphasis elements) were hardcoded. Under `.theme-light` they appeared white on a white background. The 2026-03-31 Header fix addressed the sticky header but left the footer untouched. | Replaced all three `text-white` instances with `text-text-primary` which resolves correctly through CSS variables in both dark and light mode. |
| 2026-04-15 | Mock newsletter form in index.astro sent no data | The homepage newsletter form simulated an 800ms delay then toggled a success CSS class but never POST-ed to Beehiiv or any backend. Users who subscribed via the homepage were silently dropped. | Replaced the mock form and its JavaScript with `<NewsletterEmbed />` — the same real component used in BlogLayout. |
| 2026-04-15 | Mermaid listener stacking in ExperimentLayout.astro | `<script is:inline>` re-executes on every page load (not just once per session). Each experiment page visit added another `document.addEventListener('astro:after-swap', initMermaid)` listener. On the Nth experiment page, initMermaid ran N times per swap, causing duplicate SVG renders. | Changed `<script is:inline>` to a regular module `<script>` (executes once per session). Accessed the CDN-loaded `mermaid` global as `(window as any).mermaid` to satisfy TypeScript strict mode. |
| 2026-04-15 | GiscusComments theme not synced with light reader mode | `loadGiscus()` always used `dark_dimmed` theme regardless of the user's current reader mode. When the ThemeToggle was active, Giscus rendered dark in a light page. | Added `document.body.classList.contains('theme-light')` check before injecting the Giscus script. Passes `'light'` theme when active, `'dark_dimmed'` otherwise. |
| 2026-04-15 | alert() in ContactForm.astro on submission errors | Network and Formspree API errors triggered `window.alert()` — a jarring, unstyled browser modal that breaks the UX. | Replaced both `alert()` calls with a `showError(msg)` helper that populates an inline `<p id="form-error">` element (hidden by default, auto-hides after 6s). |
| 2026-04-15 | RSS feed description used only `description` field | `rss.xml.ts` only sent `post.data.description` as the item description — the shortest and least informative field. AI readers and podcast apps received minimal context. | Updated to `post.data.aiSummary \|\| post.data.tldr \|\| post.data.description`, matching the SEO description priority chain used in BlogLayout and SEOHead. |
| 2026-04-15 | Dead inline style on PostCard.astro glow layer | The radial gradient on the glow div was set via `style="background: radial-gradient(300px ...)"` which was always overridden by a `!important` rule in the component `<style>` block. The mouse-position glow never actually showed the inline value. | Removed the inline `style` attribute entirely, added `.glow-layer` class to the div, and updated the CSS selector to target `.glow-layer` (no `!important` needed). |
| 2026-04-15 | Beehiiv iframe renders white on dark site | Beehiiv's iframe embed is cross-origin — their internal CSS forces `background: white` and it cannot be overridden from outside via CSS injection (same-origin policy). The hardcoded `height="339"` also clipped the subscribe button. User-visible symptom: jarring white box in an otherwise all-dark UI, button cut off at bottom. | Replaced the `<iframe>` in `NewsletterEmbed.astro` with a native dark-styled `<form>` that POSTs to the same Beehiiv subscribe endpoint via `fetch()` with `mode: 'no-cors'`. See decision log 2026-04-15 for no-cors trade-offs. |
| 2026-04-15 | Vite dep scanner misread JSDoc literal `<script>` substrings | Astro frontmatter JSDoc blocks in `GiscusComments.astro`, `ContactForm.astro`, and `JsonLD.astro` contained literal `<script>` tag strings in comment prose (e.g. "injects a fresh `<script>` tag on every swap"). Vite's dependency scanner treated them as real script tags and threw "Expected ';' but found 'on'" parse errors in dev mode. | Rephrased all three doc blocks to say "script tag" or "script element" without angle brackets so the scanner never sees `<script` inside frontmatter. |
| 2026-04-16 | Google Search Console: "Alternate page with proper canonical tag" on all non-root pages | Cloudflare Pages serves both `/blog` and `/blog/` as identical pages (no built-in preference). `Astro.url.href` at SSG build time produces trailing-slash pathnames (e.g. `https://gekro.com/blog/`). Google crawls `/blog` (no trailing slash), sees canonical pointing to `/blog/`, and marks `/blog` as "Alternate" rather than canonical — excluding it from indexing. | Three-part fix: (1) `SEOHead.astro` — derive canonical from `SITE_ORIGIN + Astro.url.pathname.replace(/\/$/, '')` instead of `Astro.url.href`; (2) `astro.config.mjs` sitemap `serialize()` hook strips trailing slashes from all non-root sitemap URLs; (3) `public/_redirects` 301-redirects `/*/ → /:splat` at the Cloudflare Pages edge so trailing-slash requests never reach the no-slash page as a duplicate. |
| 2026-04-17 | Build broken: `@astrojs/sitemap` 3.7.1 throws "Cannot read properties of undefined (reading 'reduce')" | Sitemap integration 3.4+ reads route data from the `astro:routes:resolved` hook, which was only added in Astro 5. With our `"^3.0.0"` spec, pnpm resolved to 3.7.1, so `_routes` stayed undefined during the Astro-4 build and `_routes.reduce` crashed in `astro:build:done`. The canonical URL fix earlier the same day did not cause this — the plugin's reduce runs regardless of `serialize` — but the canonical work was what triggered a local rebuild that surfaced the latent breakage. | Pinned `@astrojs/sitemap` to `3.2.1` (last Astro-4-compatible release) in `apps/web/package.json`, ran `pnpm install`. Until migration to Astro 5, any sitemap bump needs a paired Astro bump. |
| 2026-04-17 | Code-review sweep found self-XSS in LabTerminal command echo | `LabTerminal.astro:225` echoed the user's typed command back via innerHTML: `<span>rohit@gekro:~$</span> ${trimmed}`. Typing `<img src=x onerror=alert(1)>` executed. Self-XSS only (attacker and victim are the same user — same threat class as opening DevTools), but trivial to close. | Added an `escapeHtml()` helper on the Web Component and escape `trimmed` before interpolating into the innerHTML template. Other printLine callers either use textContent (default) or inject trusted hardcoded strings. |
| 2026-04-17 | **REGRESSION**: Every blog/experiment/topic link broken in production after canonical URL deploy | The `_redirects` rule shipped with the canonical URL fix used `/*/  /:splat  301` — a wildcard in the *middle* of the source path. Cloudflare Pages' `_redirects` format only officially supports `*` as a splat at the **end** of a source path. The middle-wildcard pattern was parsed permissively and matched `/blog/post-slug` → redirected to `/post-slug`, which 404'd, or in some cases produced redirect loops. User-facing symptom: clicking any post card on the homepage or `/blog` index returned a broken page. | Rewrote `public/_redirects` with explicit single-segment placeholder rules per route: `/blog/ → /blog`, `/blog/:slug/ → /blog/:slug`, same pattern for experiments/topics/about/contact/now/slides. `:placeholder` matches exactly one path segment per Cloudflare's documented syntax. Added an inline comment explaining the `*`-in-middle trap so the next editor doesn't repeat it. |
| 2026-04-17 | **REGRESSION 2 — ERR_TOO_MANY_REDIRECTS**: Per-route trailing-slash-stripping rules created infinite loop | Astro builds in directory format (`dist/blog/slug/index.html`). Cloudflare Pages auto-canonicalises requests for such URLs to the trailing-slash form (308 `/blog/slug` → `/blog/slug/`). The `_redirects` rule `/blog/:slug/ → /blog/:slug 301` then stripped the slash again, CF re-added it, loop. The whole trailing-slash-strip strategy from 2026-04-16 was incompatible with directory-format output. Original GSC signal ("Alternate page with proper canonical tag") was actually Google correctly respecting the canonical tag — never an indexing failure, so the 2026-04-16 fix was unnecessary. | Reverted the full canonical strategy: (1) `SEOHead.astro` back to `Astro.url.href` (natural trailing-slash); (2) removed sitemap `serialize()` hook; (3) emptied `public/_redirects` (comment-only) to let CF Pages handle canonicalisation natively. All three URL surfaces now agree on the trailing-slash form. |
