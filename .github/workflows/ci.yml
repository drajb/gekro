name: CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: [main, dev]

jobs:
  ci:
    name: Lint, Typecheck & Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          # Initialise the private apps submodule (drajb/gekro-apps).
          # SUBMODULE_PAT must be a fine-grained PAT stored in this repo's
          # Secrets with Contents:Read on drajb/gekro-apps.
          # Without this, apps-private/ is an empty directory and the build
          # step below fails with "Could not resolve …/apps-private/…".
          submodules: 'recursive'
          token: ${{ secrets.SUBMODULE_PAT }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22'   # Node 22 LTS — Node 20 LTS exited maintenance Apr 2026
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Type check
        run: pnpm --filter web exec astro check

      - name: Lint
        run: pnpm --filter web lint
        continue-on-error: true  # Don't block on lint until ESLint is fully configured

      - name: Build
        run: pnpm --filter web build
        env:
          PUBLIC_SANITY_PROJECT_ID: ${{ secrets.PUBLIC_SANITY_PROJECT_ID }}
          PUBLIC_SANITY_DATASET: ${{ secrets.PUBLIC_SANITY_DATASET }}
